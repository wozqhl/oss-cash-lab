# Changelog

All notable changes to this portfolio are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- A dry-run `registry-pack` wraps generated `mcp-server.mjs` as a local MCP Registry listing payload + tarball layout (not the generator CLI). Smoke registry-pack-ok. Never POSTs. Human still publishes.
- A generated clients send `Accept: application/json` unless already set. Smoke accept-ok.
- A generated stdio MCP servers send `Accept: application/json` on tools/call upstream HTTP unless already set. Smoke mcp-accept-ok.
- A generated stdio MCP servers apply a per-attempt 10s timeout on tools/call (AbortController / urllib timeout / context.WithTimeout; MCP_TIMEOUT_MS / MCP_TIMEOUT_SEC or SDK_TIMEOUT_*). Smoke mcp-timeout-ok.
- A generated stdio MCP servers retry 429 / 5xx / network on tools/call (max 2 retries, Retry-After <30s; Idempotency-Key reused). Smoke mcp-retry-ok.
- A generated stdio MCP servers send User-Agent / X-Request-Id / Idempotency-Key on tools/call upstream HTTP. Smoke mcp-id-ok.
- A generated clients send Idempotency-Key on POST/PUT/PATCH/DELETE when unset (retries reuse; `SDK_IDEMPOTENCY_KEY` pin). Smoke idem-ok.

- A generated clients send default User-Agent `sdk-mcp-gen/0.1.0` (or package name) unless already set, plus `X-Request-Id` new per HTTP attempt (`SDK_REQUEST_ID` pin). Smoke ua-ok / request-id-ok.

- A generated Rust / PHP / Swift / Ruby clients: per-attempt timeout + 429/5xx retry + per-op bearer/apiKey auth (stdlib; Rust http-only TcpStream). Smoke rust-auth-ok / php-auth-ok.
- E chargeback-lite: `GET /v1/tenants.csv` exports in-memory tenant totals (`tenant,spend_usd,budget_usd,remaining_usd,denied_count`); JSON `GET /v1/tenants` kept.
- E local HTML remaining-by-tenant table on `GET /` when `--tenant-budget` is set (same remaining as CSV/metrics; period label). Grafana remaining panel already scraped `otel_ai_cost_budget_remaining_usd`; no new series. Smoke `remain-dash-ok`.
- E optional UTC-day budget period (`BUDGET_PERIOD=day` / `--budget-period day`; default off) so remaining / deny / would-exceed reset at UTC midnight.
- OpenSSF Scorecard GitHub Actions workflow (`.github/workflows/scorecard.yml`) so public-repo supply-chain results can appear in code scanning.
- C DeepEval-shaped JSON adapter (`from-deepeval`; fixture-only, existing JUnit/TAP/Markdown + `--fail-under`; not full DeepEval compatibility).
- F Dify / n8n sample approval forward (`APPROVAL_FORWARD_URL` / `--forward-url`; `{event,approval_id,status,tenant|app,title}` after approved/rejected; 1 retry; example wiring, not a Dify plugin). Smoke prints `forward-ok`.
- F official-doc IM verify + send-card fixtures (`bets/f-cn-work-agent/fixtures/official-im/`; HMAC / timestamp / encrypt header names as vendors document). Existing verifier accepts DingTalk official Base64 + `timestamp`/`sign`. Mock only, no vendor network. Smoke `im-verify-ok`.
- D evidence-pack window clock: `pack.json` `clock` (listed in the zip) emits daysUntil / daysOverdue vs 2026-09-11 and 2027-12-11 from observed `--gate-vulns` hits. Calendar/evidence helper, not a CRA compliance certificate. 日历/证据辅助，不是 CRA 合格证书。Smoke `cra-clock-ok`.

## [0.1.0] - 2026-08-13

Local-MVP era: six bets green under `make smoke` / `make local-mvp` / `make stack-demo`.
Portfolio version source of truth: root [`VERSION`](./VERSION) (`make version`).

### Added

- A generated Rust / PHP / Swift / Ruby clients: per-attempt timeout + 429/5xx retry + per-op bearer/apiKey auth (stdlib; Rust http-only TcpStream). Smoke rust-auth-ok / php-auth-ok.
#### A · sdk-mcp-gen
- OpenAPI 3.x → TypeScript + Python + Go + Java + Rust + C# + Kotlin + Swift + Ruby + PHP SDK stubs + MCP tool registry + stdio MCP server（JSON / built-in YAML subset）
- `generate` with `--lang ts,python,go,java,rust,csharp,kotlin,swift,ruby,php` (default all ten); petstore fixtures; `scripts/wire-a-to-b.sh` loads tools into B
- Dogfood on B OpenAPI: `scripts/generate-gateway-sdk.sh` / `make dogfood-a-b` → `bets/b-mcp-gateway/sdk/generated/` (gitignored; hooked from `local-mvp`)
- Resolve local `#/components/schemas/*` `$ref` in requestBody when listing ops (richer MCP inputSchema for B tools/call)
- Breaking check: `check --out/--baseline` compares `mcp-tools.json` tool names (+ optional TS/Python/Go/Java/Rust/C#/Kotlin/Swift/Ruby/PHP client exports); removed/renamed → exit 1, added OK; `generate --check-baseline <dir>`; local-mvp mutate/restore proof
- Watch regenerate: `generate … --out <dir> --watch` polls OpenAPI mtime every 200ms (`fs.stat` + `setInterval`), prints `regenerated` on change; local-mvp background touch/assert/kill (5s timeout); `--check-baseline` still one-shot without watch
- Checksum manifest: `generate` writes `checksums.sha256` (sha256 of existing `client.ts` / `client.py` / `client.go` / `Client.java` / `client.rs` / `Client.cs` / `Client.kt` / `Client.swift` / `client.rb` / `Client.php` / `mcp-tools.json` / `mcp-server.mjs` / `mcp_server.py` / `mcp_server.go` / `mcp.json` / `README.md`); `verify-checksums --out <dir>` exits 0 on match, 1 on missing/mismatch; local-mvp prove (OK → tweak fail → regenerate restore); smoke self-check
- Java `Client.java` (stdlib `HttpURLConnection`, `package client`, one public method per OpenAPI operation); `--lang java`; default langs include java; local-mvp asserts file + `javac` or brace/heuristic; `--lang ts` does not emit Java
- Rust `client.rs` (stdlib `std::net::TcpStream` HTTP/1.1 for `http://` only, no TLS; snake_case `pub fn` per operation); `--lang rust` / `rs`; default langs include rust; local-mvp asserts file + `rustc --crate-type lib` or brace/heuristic; `--lang ts` does not emit Rust; dogfood A→B/C/D/E/F stay `--lang ts,python,go`
- C# `Client.cs` (stdlib `System.Net.Http.HttpClient`, classic `namespace Client` + `public class Client`, one public PascalCase method per OpenAPI operation); `--lang csharp` / `cs` / `c#`; default langs include csharp; local-mvp asserts file + `dotnet`/`csc` or brace/heuristic (char literals before strings); `--lang ts` does not emit C#; dogfood A→B/C/D/E/F stay `--lang ts,python,go`
- Kotlin `Client.kt` (`class Client`, JVM stdlib `java.net.HttpURLConnection`, okhttp-free, one `fun` per OpenAPI operation); `--lang kotlin` / `kt`; default langs include kotlin (after csharp); local-mvp asserts file + `kotlinc` or brace/heuristic (char literals before strings); `--lang ts` does not emit Kotlin; dogfood A→B/C/D/E/F stay `--lang ts,python,go`
- Swift `Client.swift` (`public class Client`, Foundation `URLSession`, Alamofire-free, one `public func` per OpenAPI operation, single file / no SPM); `--lang swift`; default langs include swift (after kotlin); local-mvp asserts file + `swiftc -typecheck` or brace/heuristic; `--lang ts` does not emit Swift; dogfood A→B/C/D/E/F stay `--lang ts,python,go`
- Ruby `client.rb` (`class Client`, stdlib `Net::HTTP`, gem-free, one public snake_case method per OpenAPI operation); `--lang ruby` / `rb`; default langs include ruby (after swift); local-mvp asserts file + `ruby -c` or brace/heuristic; `--lang ts` does not emit Ruby; dogfood A→B/C/D/E/F stay `--lang ts,python,go`
- PHP `Client.php` (`class Client`, stdlib `fopen` / stream wrappers, curl-extension-free, one public camelCase method per OpenAPI operation); `--lang php`; default langs include php (after ruby); local-mvp asserts file + `php -l` or brace/heuristic; `--lang ts` does not emit PHP; dogfood A→B/C/D/E/F stay `--lang ts,python,go`
- Generate `--dry-run`: `generate <spec> --out <dir> --dry-run` prints `{files, operations, tools, langs, packageName}` JSON and writes nothing (no mkdir); local-mvp proves empty/unchanged `--out` and default langs list includes `client.ts` / `client.py` / … / `checksums.sha256`; smoke plan matches `generate` file list; `--watch` / `--check-baseline` skipped on dry-run
- Stdio MCP server: `generate` writes `mcp-server.mjs` (default; `--mcp` no-op). Node >= 18, no extra deps. Newline JSON-RPC `initialize` / `tools/list` / `tools/call` matching B mock-upstream / stdio proxy. Tools from OpenAPI `operationId`; simplified inputSchema (string/number/boolean/object); handlers `fetch` `--base-url` / env `MCP_BASE_URL` with path-param substitution. `--dry-run` lists the file; `--watch` regenerates it; checksums include it. smoke + local-mvp one-shot stdin `tools/list` prove tool names; PHP/Ruby/Java/etc clients unchanged
- Python stdio MCP server: `generate` also writes `mcp_server.py` next to `mcp-server.mjs` (default on). Python 3, stdlib `urllib` only (no `requests`). Same JSON-RPC surface and tool names as the Node server so `tools/list` matches (`listPets`, …). `--dry-run` lists it; `--watch` regenerates it; checksums include it. smoke + local-mvp stdin `tools/list` prove names match JS; dogfood A→B asserts the file; JS MCP + PHP/Ruby/Java/etc clients unchanged
- Go stdio MCP server: `generate` also writes `mcp_server.go` (`package main`) next to the JS/Python servers (default on). Go 1.21+, stdlib `net/http` + `encoding/json` only (no extra modules). Same JSON-RPC surface and tool names so `tools/list` matches (`listPets`, …). Path-param substitution like JS. `--dry-run` lists it; `--watch` regenerates it; checksums include it. Generate does not require `go`. smoke + local-mvp: file + JSON-RPC method strings; `go run mcp_server.go` stdin `tools/list` when `go` is on PATH. dogfood A→B asserts the file; JS/Python MCP + PHP/Ruby/Java/etc clients unchanged
- `--package-name NAME` (alias `--package`; env `SDK_PACKAGE_NAME`): company module path for generated clients. Default `client` so petstore `package client` / `namespace Client` / `listPets` greps still pass. Applies to TS `package.json` name, Python `__package_name__` (+ `{name}/__init__.py` when customized), Go `package` + module path suffix, Java/Kotlin `package`, C# namespace, Ruby gem comment, PHP namespace. `--dry-run` prints `packageName`. MCP servers keep `mcp-server.mjs` / `mcp_server.py` / `mcp_server.go`. Checksums hash file contents (now include `package.json` when TS is emitted). smoke + isolated local-mvp `--package-name acme_pets`.
- **OpenAPI 3.1.x**: `generate` accepts `openapi: 3.1.0` (and 3.1.x) alongside 3.0.x. Cheap diffs only — `type: [string, "null"]` → string optional; schema/media `examples` treated like `example`; local `$ref` still resolved; `webhooks` ignored (not SDK/MCP tools). Not a JSON Schema 2020-12 rewrite. Petstore 3.0.3 fixtures unchanged. Mini fixture `bets/a-sdk-mcp-gen/examples/openapi-3.1-mini.json`; smoke generates TS+Python + `tools/list` includes `listItems`; isolated local-mvp prove. `--package-name`, checksums, dry-run, JS/Python MCP unchanged.
- **`generate --url`**: fetch OpenAPI from `http://` / `https://` / `file://` (Node 18+ `fetch`; **10s** timeout, **2MB** max body, follow ≤3 redirects). **XOR** with the file operand (both set → exit 2). Non-2xx → exit 1 with status. Other schemes rejected. `--dry-run` still lists outputs after a successful fetch. `--watch --url` polls the remote spec (see below). Optional block of `169.254.169.254`. No extra deps. Default petstore local-mvp stays a file path; smoke `file://` + isolated `python -m http.server` prove; `--package-name`, MCP js/py, OAS 3.1 unchanged.
- **`generate --url --header`**: repeatable `--header "Name: value"` plus optional env `SDK_FETCH_HEADER` (single) on http(s) `--url` so private OpenAPI can be fetched (`Authorization: Bearer …`). Error if `--header` without `--url` (exit 2). Extra headers are **not** sent to `file://`. Timeout / size / redirect / `169.254.169.254` blocks unchanged. Authorization values are redacted in errors; `--dry-run` lists header **names** only (never values). No extra deps; do not commit real tokens. smoke + isolated local-mvp loopback 401 without header / 200 with `Bearer test-token`; XOR url+file still 2; petstore file path unchanged.
- **`generate --watch --url`**: `--watch` with `--url` polls the remote spec (default **2000ms**, `--watch-interval-ms`; file watch stays **200ms**). `If-None-Match` when an ETag is present (**304** → skip write); else SHA-256 body hash (and `If-Modified-Since`). `--header` / `SDK_FETCH_HEADER` apply to polls; secrets never logged. Fetch failures log once and keep the previous generation (no crash). Change regenerates + `checksums.sha256` like file watch. smoke: in-process 304 skip + hash-change regen. Isolated local-mvp `python -m http.server` mutate → `regenerated` log → kill, no hang. File-watch petstore prove unchanged. 10s poll timeout; `169.254.169.254` blocks unchanged.
- **MCP client config snippet**: `generate` writes `mcp.json` next to the stdio servers (`mcpServers` keyed by `--package-name` or OpenAPI title slug; `command: node`, relative `args: ["./mcp-server.mjs"]`, `env.MCP_BASE_URL`; optional `…-py` / `…-go`). Paste into MCP servers config JSON (Cursor / Claude Desktop / Claude Code). `--dry-run` lists the file; checksums include it; `--no-mcp` skips servers + snippet. smoke petstore/mini + local-mvp JSON parse. Existing clients/MCP unchanged. Does not require Cursor.
- **`generate --zip`**: emit `sdk.tgz` (gzip tarball when tar is on PATH) or store-only `sdk.zip` (Node stdlib, no extra dep). Written after `checksums.sha256`; checksums do not list the archive (the archive includes checksums). `--dry-run --zip` lists the archive name and writes nothing. Default generate without `--zip` unchanged. smoke: archive exists; listing includes mcp-server.mjs or client.ts. local-mvp petstore `--zip` lists mcp.json.
- **Generated `LICENSE` + `NOTICE`**: default `generate` writes Apache-2.0 `LICENSE` (portfolio root text when present, else a short Apache-2.0 stub) and a generated `NOTICE` naming `--package-name` / default `client`, optional “based on OpenAPI from …” (spec title/url only — never `--header` secrets), and a pointer to `LICENSE`. Always overwritten (generated artifacts). `--no-license` skips both (dry-run `files` + checksums omit them; leftovers unlinked). `--no-mcp` still writes them. Checksums include them when present; `--zip` archives include them. smoke + local-mvp petstore prove; no new deps.
- **Generated `.gitignore`**: default `generate` writes a consumer `.gitignore` (`__pycache__/`, `*.pyc`, `node_modules/`, `.DS_Store`, `*.egg-info/`) so a committed SDK does not pick up build junk. Always overwritten. `--no-gitignore` skips it (dry-run `files` + checksums omit it; leftovers unlinked). Independent of `--no-mcp` / `--no-license`. Checksums include it when present; `--zip` archives include it unless skipped. smoke + local-mvp petstore prove; no new deps.
- **GitHub Actions OpenAPI drift example:** copy-paste [`examples/github-actions/sdk-mcp-gen-check.yml`](./examples/github-actions/sdk-mcp-gen-check.yml) (`node src/cli.js generate examples/petstore.openapi.json --out sdk` then `generate … --out sdk-new --check-baseline sdk`; exit 1 on removed/renamed tools; optional two-step `check --out NEW --baseline sdk`). Composite [`examples/github-actions/sdk-mcp-gen-check/action.yml`](./examples/github-actions/sdk-mcp-gen-check/action.yml). Parse-only `scripts/check-gha-examples.sh` on smoke (petstore generate twice, happy-path exit 0). Not a required workflow in this repo.


#### B · mcp-gateway
- Enterprise MCP gateway: allow/deny, rate-limit, JSONL audit, HTTP/stdio upstream proxy + mock-upstream
- Tenant API-key sketch, hot reload, audit query + **audit export packs** (JSON/CSV HTTP + offline CLI)
- Optional per-tenant **`ipAllowlist`** (exact IPv4/IPv6 + IPv4 CIDR `/8`/`/16`/`/24`); client IP from `X-Forwarded-For` first hop or socket; mismatch → `403 {error:"forbidden",reason:"ip_denied"}`; Prometheus `ip_denied_total`
- Request **`maxBodyBytes`** (default 1048576; optional per-tenant override): oversized POST body → `413 {error:"payload_too_large"}` before JSON parse (Content-Length early or stream/count); Prometheus `body_too_large_total`
- **Admin tenant inventory**: `GET /admin/tenants` (admin token) → `id`, allow/deny counts, `rateLimit`, `hasIpAllowlist`, `maxBodyBytes`, `apiKeyMasked` (last 4 only; **never** raw `apiKey`); missing/invalid token → `401`; OpenAPI + local-mvp proof
- **Admin single tenant**: `GET /admin/tenants/{id}` (same admin token) → `{ok, id, hasApiKey, hasPreviousApiKey, previousApiKeyExpiresAt, allow, deny, rateLimit, hasIpAllowlist, maxBodyBytes}`. **Never** `apiKey` / `previousApiKey` values. Unknown id → **404** `{error:tenant_not_found}`; unauth → **401**. CORS + `X-Request-Id`. OpenAPI `adminGetTenant`. smoke helper (JSON.parse + planted `apiKey` fails) + HTTP 401/200/404; local-mvp curl 200 + OpenAPI + 401 + 404; stack-demo OpenAPI + unauth 401
- **Admin tenant API token rotation**: `POST /admin/tenants/{id}/rotate` (alias `POST /admin/tenants/rotate` body `{tenantId}`; same admin auth) → `{ok:true, tenantId, token, previousTokenExpiresAt?}` — new token shown once. Previous key valid for grace (default **60s**, `--rotate-grace-sec` / `TOKEN_ROTATE_GRACE_SEC`; `0` = immediate). During grace both keys work; after grace only the new. Unknown tenant → `404`; unauth → `401`. Audit `type=token_rotated` (**no** raw tokens in JSONL/CSV/logs/metrics). File-backed policies persist `apiKey` (+ previous during grace) the same way existing keys are stored; in-memory-only documented. smoke helper + isolated local-mvp; stack-demo OpenAPI path only (does not rotate demo `acme`)
- **CORS**: `cors: { origins: ["*"] or list, methods, headers }`; default omit/empty origins = no CORS headers. OPTIONS preflight: allowed Origin → 204 + ACAO; explicit-list miss → 403 `cors_denied`. GET/POST include ACAO when Origin matches. local-mvp `origins: ["http://localhost:3000"]` (evil origin fails); OpenAPI `CorsConfig`
- **CORS expose Retry-After**: default `Access-Control-Expose-Headers` includes `Retry-After` (keeps `X-Request-Id`); GET/OPTIONS local-mvp prove (case-insensitive); OpenAPI `CorsConfig` note; allowlist/deny unchanged
- OpenAPI 3 (`openapi/gateway.openapi.json`, `GET /openapi.json`) with ApiKey/Bearer schemes; Prometheus `GET /metrics` (`tool_calls_total`, `rate_limited_total`, `ip_denied_total`, `body_too_large_total`, `upstream_timeout_total`, `circuit_open_total`, `webhook_retries_total`, `http_requests_total`)
- **Upstream request timeout**: `upstream.timeoutMs` (default 5000). HTTP `fetch` AbortController + stdio pending RPC abort after the window. `POST /tools/call` → `504 {error:"upstream_timeout"}`; audit `reason=upstream_timeout`; Prometheus `upstream_timeout_total`. local-mvp isolated delay mock (`--delay-ms`) proves 504; existing HTTP/stdio proxy uses generous timeout
- **Upstream circuit breaker**: optional `upstream.breaker: { failureThreshold: 3, openMs: 2000 }` (omit / `enabled:false` / threshold 0 = disabled; defaults when present). Counts `upstream_timeout` and 5xx/connect errors. After threshold, open: `POST /tools/call` → `503 {error:"circuit_open"}` without calling upstream until `openMs` (half-open: one probe). Audit `reason=circuit_open`; Prometheus `circuit_open_total`. local-mvp isolated delay mock + `failureThreshold: 1` proves fast 503 then probe 200; existing timeout/proxy proves keep breaker disabled
- **GET /health circuit snapshot**: when `upstream.breaker` is enabled, health JSON includes `breaker: { state: closed|open|half_open, failures, openUntil }` (ISO-8601 while open, otherwise `null`; **no secrets**). Field omitted when disabled so stack-demo `/health` `{ok:true}` is unchanged. OpenAPI `Health` + `CircuitBreakerSnapshot`. Isolated local-mvp: after open → `state:open`; after `openMs` → `half_open`; after successful probe → `closed`
- **GET /ready (readiness):** 200 `{ok:true}` when breaker disabled or `closed`/`half_open`; 503 `{ok:false, reason:"circuit_open"}` when `state` is `open`. Includes `breaker` snapshot when enabled (same shape as `/health`; **no secrets**; `Retry-After` on 503). Liveness `GET /health` stays 200 `{ok:true}` even if the circuit is open. Compose/stack-demo healthchecks stay on `/health`. OpenAPI `Ready` + `getReady`. Isolated local-mvp: after open → `/health` 200 + `/ready` 503; after recovery → `/ready` 200
- **503 Retry-After on circuit_open**: remaining seconds until `openUntil` (min 1). Isolated local-mvp 503 prove asserts integer `Retry-After` ≥ 1. OpenAPI `CircuitOpen` headers (`Retry-After`, `X-Request-Id`). Health snapshot keys unchanged
- Optional **audit webhook fan-out**: `webhooks: [{ url, events: ["tool_call","deny"], secret }]` POST after audit write (fire-and-forget, short timeout; never fails tool call); `webhooksRedact` default true (+ respects `redactOnWrite` / `export.redactDefault`); optional **HMAC-SHA256** (`webhooks[].secret` → `X-Webhook-Signature: sha256=<hex>` of raw body); local-mvp mock receiver prove (unsigned fan-out + isolated signed receiver); **exponential backoff / queues, key rotation, timestamp replay protection = paid**
- **Outbound webhook timestamp (OSS):** every audit webhook POST includes `X-Webhook-Timestamp: <unix-seconds>` (with or without HMAC). HMAC still signs the **raw JSON body only** (existing HMAC proves unchanged). Mock receiver records the header; local-mvp asserts present and roughly now. **Timestamp replay window enforcement = paid later**
- **Audit webhook 1 retry (OSS):** on 5xx or network/timeout, retry the POST once after ~50ms (success on first try = no retry; 4xx do not retry). Optional Prometheus `webhook_retries_total`. HMAC/timestamp/redact unchanged. Isolated local-mvp `--fail-once` mock (500 then 200) plus smoke unit-tests. **Exponential backoff / queues = paid**
- **`X-Request-Id`**: accept incoming header or generate UUID; echo on every response; store `requestId` on audit JSONL events, `GET /audit` / export (JSON+CSV) and webhook payloads; OpenAPI parameter/header + `AuditEvent`; local-mvp custom-id proof
- **Gzip audit export**: `GET /audit/export?format=json|csv&gzip=1` or `Accept-Encoding: gzip` returns a gzipped body with `Content-Encoding: gzip` and `Content-Disposition` filename `.json.gz` / `.csv.gz`. Explicit `gzip=0` keeps uncompressed even if Accept-Encoding includes gzip. CLI `export-audit --gzip --out out/audit.json.gz`. Uncompressed JSON/CSV packs unchanged. OpenAPI + local-mvp (`gzip -t` / zlib decompress) proof
- **Serve `--watch`:** `serve --config policy.json --watch` polls config mtime every 300ms (`fs.stat` + `setInterval`) and reloads policy via the same `reloadPolicyAndUpstream` path as SIGHUP / `POST /admin/reload`. Parse errors keep the previous policy; prints `regenerated` on success. local-mvp isolated copy prove (curl `tools/list` → add allow tool → list / `GET /admin/tenants` reflect change → kill, no hang). Main serve / stack-demo unchanged (no `--watch`). Existing SIGHUP / `/admin/reload` proves unchanged
- **Graceful SIGTERM/SIGINT drain:** always on. `/ready` immediately **503** `{ok:false, reason:"shutting_down"}` (wins over healthy 200 / circuit_open); `/health` stays **200** with `shuttingDown: true` (omitted otherwise). Skip new upstream `tools/call`. Drain default **5s** (`--drain-ms` / `SHUTDOWN_DRAIN_MS`, cap 30s); log `shutting down` then `exit`. OpenAPI `Ready` reason enum includes `shutting_down`. Isolated local-mvp `kill -TERM` prove; smoke in-process `beginShutdown`
- **JSON access logs (opt-in):** `serve --log-json` or env `LOG_FORMAT=json` (env wins if CLI omitted) — one stdout JSON line per completed app request `{ts,level:info,msg:http,service,method,path,status,durationMs,requestId}` (optional `bytesOut`/`remote`; no secrets/headers/bodies). Skips `/health` `/ready` `/metrics` and OPTIONS. `requestId` matches `X-Request-Id`. Default **off**. Isolated local-mvp `--log-json` curl `X-Request-Id: test-log-1`; smoke formats the line
- **Admin audit CSV (SIEM):** `GET /admin/audit.csv` + `GET /admin/audit?format=csv|json` (admin token; **not public**; tenant keys 401). Columns `ts,tenantId,tool,allow,reason,via,requestId` (no args/headers/tokens/bodies). Empty → header only 200. CORS + `X-Request-Id`. Gzip JSON/CSV packs on `GET /audit/export` unchanged. CLI `export-audit --format csv` already exists. smoke helper (ok+error, secrets absent); local-mvp curl 200/401
- **Admin audit Markdown (SIEM):** `GET /admin/audit.md` + `GET /admin/audit?format=md` (same admin token gate as CSV; tenant keys 401). `text/markdown`; `# Audit` + GFM table; same columns as admin CSV; `|` escaped; **no** args/headers/tokens/bodies. Empty → heading + header 200. Ring cap applies. CLI `export-audit --format md` (json/csv/gzip unchanged). smoke helper (2 events, secrets absent) + in-process 401; local-mvp curl 200/401 + token absent; stack-demo OpenAPI path only (no admin token curl)
- **Admin audit HTML (local demo):** `GET /admin/audit.html` + `GET /admin/audit?format=html` (same admin token gate as CSV/Markdown; tenant keys 401). `text/html; charset=utf-8`; self-contained table (no CDN); same columns as admin CSV; all text escaped; deny vs allow rows distinct (inline CSS); **no** args/headers/tokens/bodies. Empty → heading + “no events” + header 200. CLI `export-audit --format html` prints HTML to stdout (offline JSONL; no secrets). Gzip stays on JSON/CSV `/audit/export`. smoke helper (`<table` + escaped reason) + empty 200 + HTTP 401/200; local-mvp curl 200 `text/html` after a tool call + 401 + OpenAPI path; stack-demo OpenAPI `getAdminAuditHtml` / `format=html` + unauth 401
- **Streamable HTTP MVP (POST JSON-RPC, not full SSE):** `POST /mcp` (alias `POST /`) accepts JSON-RPC `initialize` / `tools/list` / `tools/call` with the same tenant API key as REST `/tools/*`. Echoes `MCP-Protocol-Version` (default `2025-03-26`). Assigns `Mcp-Session-Id` UUID on initialize if omitted (also `result.sessionId`); later calls may omit the session (backward compatible). `GET /mcp` → **405** `Allow: POST, DELETE` (no hanging SSE). REST `/tools/list` + `/tools/call` unchanged. OpenAPI `mcpJsonRpc` / `mcpStreamableGet`. smoke in-process + local-mvp `curl -D-` prove session + protocol headers
- **Streamable HTTP session TTL:** unused `Mcp-Session-Id` expires after **3600s** (`--session-ttl` / `MCP_SESSION_TTL_SEC`; `0` = no expiry, ids still generated). In-memory last-seen map (cap **10000**, drop oldest). Expired id → HTTP **404** `{error:"session_expired"}` (re-init). Missing session still allowed; unknown ids accepted and tracked. REST `/tools/*` / rotate / audit cap / rate limit unchanged. smoke helper (1ms expire + missing 200) + isolated local-mvp `--session-ttl 1` then `0`; stack-demo default 3600, curls without session still 200
- **Streamable HTTP session terminate:** `DELETE /mcp` with `Mcp-Session-Id` drops that session (**204** No Content). Missing header → **400** `{error:"session_id_required"}`. Unknown / expired / already gone → **404** `{error:"session_not_found"}` (idempotent; does not leak existence). Session id is the capability (no admin token). POST with the same id afterwards → **404**. `GET /mcp` 405 `Allow: POST, DELETE`. CORS default methods include DELETE. Optional audit `type=session_deleted` (no raw session id / tokens). REST `/tools/*` unchanged. OpenAPI `mcpSessionDelete`. smoke + isolated local-mvp `--session-ttl` + stack-demo OpenAPI + DELETE without session → 400
- **Admin session inventory:** `GET /admin/sessions` (same admin token as other `/admin/*`) → `{ok, ttlSec, cap, count, sessions:[{id, ageMs, ttlRemainingMs, lastSeen}]}` live in-memory Streamable HTTP sessions. Tombstones omitted; array capped at **100** newest-by-lastSeen (`count` is the full live size; `truncated: true` when more); optional `dropped` tombstone count; **no** secrets/API keys/Authorization. `ttlSec` `0` → `ttlRemainingMs` may be `null`. Unauth → **401**. CORS + `X-Request-Id`. OpenAPI `adminListSessions`. Admin force-drop is `DELETE /admin/sessions/{id}` (`DELETE /mcp` remains the client capability path). smoke 401/empty/init/DELETE; local-mvp main + isolated + CORS OPTIONS; stack-demo OpenAPI + unauth 401 (no leak)
- **Admin session force-drop:** `DELETE /admin/sessions/{id}` (same admin token; id in the path, **no** `Mcp-Session-Id` header) → **204** No Content (tombstone; subsequent `POST /mcp` with that id → **404** `session_not_found`). Unknown/expired/already gone → **404** `{error:session_not_found}` (idempotent). Missing path id → **404** (router-style). Unauth → **401**. Optional audit `type=session_deleted` `via: admin` (no raw session id). CORS Allow-Methods include DELETE. Client `DELETE /mcp` unchanged. OpenAPI `adminDeleteSession`. smoke initialize → admin DELETE 204 → POST 404; second DELETE 404; unauth 401. local-mvp main + isolated + CORS OPTIONS DELETE. stack-demo OpenAPI + unauth DELETE 401 (no leak)
- **Admin webhook inventory:** `GET /admin/webhooks` (same admin token) → `{ok, count, webhooks:[{id, events, hasUrl, hasSecret}]}` redacted outbound webhook list for platform/security buyers. **Never** `url` / `secret` / apiKey / previousApiKey / admin token / Authorization / `--header` values / tenant tokens. Empty → `{ok:true, count:0, webhooks:[]}`. Unauth / tenant API key → **401**. CORS + `X-Request-Id`. OpenAPI `adminListWebhooks`. smoke helper (JSON.parse + planted `url`/`secret` fails) + HTTP 401/empty 200/one-hook 200; local-mvp curl 200 + OpenAPI + 401; stack-demo OpenAPI + unauth 401
- **Admin runtime config:** `GET /admin/config` (same admin token) → `{ok, sessionTtlSec, sessionCap, auditMax, rotateGraceSec, rateLimit, cors.origins, upstream.timeoutMs/breaker, tenants.count, webhooks.count + hasWebhookSecret}` redacted runtime knobs for pilot debugging. **Never** `apiKey` / `previousApiKey` / webhook secrets / admin token / Authorization / `--header` values / tenant tokens. Unauth → **401**. CORS + `X-Request-Id`. OpenAPI `adminGetConfig`. smoke helper (JSON.parse + planted `apiKey` fails) + HTTP 401/200; local-mvp curl 200 + OpenAPI + 401; stack-demo OpenAPI + unauth 401
- **MCP client config example:** [`examples/mcp/gateway.mcp.json`](./examples/mcp/gateway.mcp.json) (`url` `http://127.0.0.1:8787/mcp` + `headers.Authorization` Bearer `${env:MCP_GATEWAY_TOKEN}`; not stdio). Cursor remote MCP uses `url`+`headers`. POST-only (no SSE). Parse-only smoke hook. Does not start Cursor.
- **In-memory audit ring buffer:** live `GET /audit` / `/audit/export` / `/admin/audit` retain at most **10000** events (default; `--audit-max` / `AUDIT_MAX_EVENTS`). Over cap, drop oldest. `0` = unlimited (**dangerous**). Webhook fan-out still posts every new event. Prometheus gauges `audit_events` / `audit_retained`. smoke: push 5 with max=3 → length 3 + CSV 3 data rows; isolated local-mvp `--audit-max 2` → admin CSV 2 rows; main demo default 10k export unchanged

#### C · agent-ci
- Deterministic fixture/seeded eval runner with JUnit, baseline diff, suite import (dir/zip)
- Local hosted-runner stub (`serve` HTTP API + Bearer `demo` paid-seat sketch + JUnit artifact)
- **GitHub Actions JUnit example:** copy-paste [`examples/github-actions/agent-ci-junit.yml`](./examples/github-actions/agent-ci-junit.yml) (`python3 -m agent_ci run --suite fixtures/demo --junit junit.xml` + `actions/upload-artifact@v4`; optional `publish-unit-test-results` comment; optional run-vs-run `diff --from run-a.json --to run-b.json --format md >> "$GITHUB_STEP_SUMMARY"` — two identical demo dumps → “no changes”; previous artifact as `--from`). Composite [`examples/github-actions/agent-ci-junit/action.yml`](./examples/github-actions/agent-ci-junit/action.yml). Parse-only `scripts/check-gha-examples.sh` on smoke. Not a required workflow in this repo.
- **JUnit XML export (OSS):** `run --format junit` stdout XML; `GET /v1/runs/{id}/junit.xml` + `GET /v1/runs/junit.xml` (`application/xml`, escaped `& < > "`; empty → `tests="0"`; reuses run `summary.cases` / pass/fail/error). smoke fake pass+fail `&amp;`; local-mvp curl 200 + `<testsuite`. JSON run APIs unchanged
- **TAP13 export (OSS):** `run --format tap` stdout TAP version 13; `GET /v1/runs/{id}/tap.txt` (alias `/tap`) + `GET /v1/runs/tap.txt` (`text/plain; charset=utf-8`; empty → `1..0`; `#` in names escaped; fail → `not ok` + `#` diagnostic; gate `below_threshold` → `not ok`). Same CaseResult / run record as JUnit. smoke fake pass+fail `&`/`#`; local-mvp curl 200 + `TAP version 13`; stack-demo empty aggregate `1..0`. `--format junit` / `--junit PATH` / queue / webhook / rate-limit unchanged
- **Markdown run report (OSS):** `run --format md` stdout Markdown for GitHub Actions `$GITHUB_STEP_SUMMARY` / humans (`# agent-ci: <suite>` + **Status** / **Score** / **Gate** + `| case | status | time |`); `GET /v1/runs/{id}/report.md` (alias `/md`) + `GET /v1/runs/report.md` (`text/markdown; charset=utf-8`; empty aggregate → heading + no data rows; `|` in names escaped; gate fail → `fail`). Same CaseResult / run record as JUnit/TAP. smoke fake pass+fail `|` escaped; local-mvp curl 200 + `#`; stack-demo empty or OpenAPI path. JUnit / TAP / fail-under / runs-max unchanged
- **GitHub Actions workflow-command annotations (OSS):** `run --format gha` (alias `--format annotations`) stdout `::error title=<suite>/<case>::<message>` (pass-only empty / no `::error`; gate fail → `::error title=gate::score N < failUnder M`; `%`/CR/LF escaped). `GET /v1/runs/{id}/annotations.txt` (alias `/annotations`) + `GET /v1/runs/annotations.txt` (`text/plain; charset=utf-8`; empty aggregate → empty). Same CaseResult / run record as JUnit/TAP/Markdown. Does not require GitHub. smoke fake fail `::error` + pass-only none; local-mvp `--format gha` on drift/failUnder 101 + curl 200; stack-demo OpenAPI path. JUnit / TAP / Markdown unchanged
- **Run-vs-run baseline diff (OSS):** `GET /v1/runs/{id}/diff?against={otherId}` + CLI `diff --from a.json --to b.json` compare two **completed** runs by case identity `suite/name` → `{ok,from,to,added,removed,regressed,fixed,unchanged}` (`regressed` = pass→fail, `fixed` = fail→pass). Missing → **404**; incomplete → **409** `{error:run_not_done}`. Empty both → empty arrays + `unchanged` 0. CORS + `X-Request-Id`. OpenAPI `getRunDiff`. Distinct from `--diff-baseline` (trajectory/score snapshot). Does not change fail-under scoring. smoke: extra-fail `regressed` + identical empty diffs; local-mvp POST two + curl 200; stack-demo OpenAPI path (fake ids may 404)
- **Run-vs-run baseline diff Markdown (OSS):** CLI `diff --from/--to --format md` + `GET /v1/runs/{id}/diff.md?against=` (alias `?format=md`) → `text/markdown; charset=utf-8` for `$GITHUB_STEP_SUMMARY` (heading + counts + GFM tables; `|` escaped; empty → heading + “no changes”). Same 404/409/400 as JSON. CORS + `X-Request-Id`. OpenAPI `getRunDiffMd`. JSON path unchanged. smoke extra-fail `regressed`/`demo/flaky` + identical “no changes”; local-mvp curl 200 `text/markdown` after two POSTs; stack-demo OpenAPI path
- **Run-vs-run baseline diff HTML (OSS):** CLI `diff --from/--to --format html` + `GET /v1/runs/{id}/diff.html?against=` (alias `?format=html`) → `text/html; charset=utf-8` for the local 5-min demo (heading + counts + tables; names escaped `html.escape`; regressed rows red via inline CSS; empty → heading + “no changes”; no CDN). Same 404/409/400 as JSON (error bodies stay JSON). CORS + `X-Request-Id`. OpenAPI `getRunDiffHtml`. JSON/Markdown unchanged. smoke extra-fail `<table` + `flaky` escaped + identical “no changes”; local-mvp curl 200 `text/html` after two POSTs; stack-demo OpenAPI path
- **HTML run report (OSS):** `run --format html` stdout self-contained HTML table for the local 5-min demo (`<h1>agent-ci: <suite></h1>` + Status / Score / Gate + suite/case/status/time/message; fail status red via inline CSS; no CDN). `GET /v1/runs/{id}/report.html` (alias `/html`) + `GET /v1/runs/report.html` (`text/html; charset=utf-8`; empty aggregate → heading + “no runs”; names escaped `& < > "`; gate fail → `fail`). Same CaseResult / run record as JUnit/TAP/Markdown. smoke fake pass+fail escaped names + `<table` + fail status; empty HTML 200; local-mvp curl `/v1/runs/report.html` 200 + `text/html` + OpenAPI path; stack-demo empty 200 + OpenAPI `report.html` / `getRunHtml`. JUnit / TAP / Markdown / GHA unchanged
- **Quality gate (OSS):** `run --fail-under N` (0–100). Score = `passed/total * 100` (0 if total=0). `score < N` → CLI exit 1. Default omit = today's exit. HTTP POST `/v1/runs` optional `failUnder`; completed JSON includes `score` and `gate: {failUnder, passed}` when set. Gate fail → status **`failed`**, `error: "below_threshold"` (JUnit `failures>=1`; synthetic `below_threshold` case if all cases passed). Webhook payload includes `score` + `gate` when present (HMAC unchanged). smoke: 2-pass fail-under 80 exit 0; 1-pass/1-fail score 50 fail-under 80 exit 1; no flag exit 0. local-mvp: POST `failUnder: 101` against demo → status failed + score; without failUnder still `done`
- **Local in-memory run queue (OSS):** `serve --concurrency N` (default 1) + `--max-queue` (default 16); POST `/v1/runs` → 202 `{runId,status}` (`queued` → `running` → `done` or `error`); poll GET; queue full → **429** + `Retry-After`; **hosted autoscaling = paid later**
- **Suite catalog API (OSS):** `GET /v1/suites` + `GET /v1/suites/{name}` list `fixtures/` dirs as `{name,path,caseCount}` (cassette `*.json` count); local-mvp asserts `demo` with `caseCount>=1`
- **Run list API (OSS):** `GET /v1/runs` (exact path; `?limit=` default 20) lists recent `data/runs/*.json` as `{runId,status,createdAt,summary.{passed,failed,total}}`; does not shadow `GET /v1/runs/{id}`; local-mvp asserts posted `runId` appears in the list
- GitHub Check Run adapter skeleton: `report-check` writes local Checks-shaped JSON (`out/check-run.json`); optional `--post-url` to local mock `POST /v1/check-runs`; real GitHub token posting = paid/hosted later
- **CORS**: `serve --cors-origins` CSV or env `AGENT_CI_CORS_ORIGINS` (`*` allowed); default empty/omit = no extra CORS headers. OPTIONS preflight: allowed Origin → 204 + ACAO; explicit-list miss → 403 `cors_denied`. GET/POST include ACAO when Origin matches. Default allow/expose headers include `X-Request-Id`. local-mvp isolated prove `origins: ["http://localhost:3000"]` (evil origin fails); existing queue/junit/check-run/suites/runs curls unchanged
- **CORS expose Retry-After**: default `Access-Control-Expose-Headers` includes `Retry-After` (keeps `X-Request-Id`); GET/POST/OPTIONS local-mvp prove (case-insensitive); OpenAPI `CorsConfig` note; allowlist/deny unchanged
- **HTTP rate limit:** `--rate-limit` / env `RATE_LIMIT_PER_MINUTE` (alias `RATE_LIMIT_RPM`) default **120**; in-memory sliding window by client IP (`X-Forwarded-For` first hop, else socket); exceed → **429** `{ok:false, reason:"rate_limited"}` + `Retry-After`; skip `/health` `/ready` `/metrics`; distinct from queue-full 429; `0` = unlimited; smoke helper N+1; isolated local-mvp `--rate-limit 2` (third `/v1/suites` 429, `/health` 200); stack-demo default generous (no accidental 429)
- **`X-Request-Id`**: accept incoming header or generate UUID; echo on every response (incl 4xx/OPTIONS); store `requestId` on POST `/v1/runs` records, `GET /v1/runs/{id}` and list rows; CORS allow/expose `X-Request-Id`; local-mvp custom-id proof (response header + stored run)
- **Run-complete webhook (OSS):** `serve --webhook-url` or env `AGENT_CI_WEBHOOK_URL`; after a run reaches `done`|`error`, fire-and-forget POST JSON `{runId,status,summary,requestId,conclusion}` (short timeout; never fails the run); local-mvp mock receiver (`mock-webhook-receiver.py`) writes last body and asserts `runId`
- **Run-complete webhook HMAC (OSS):** optional `--webhook-secret` / env `AGENT_CI_WEBHOOK_SECRET`. When set, POST includes `X-Webhook-Signature: sha256=<hex>` HMAC-SHA256 of the raw JSON body. Mock receiver optional `--secret` verify + `--headers-out`. Unsigned prove unchanged; isolated signed receiver in local-mvp. Simple HMAC is OSS; **key rotation / timestamp replay protection = paid later**
- **Outbound webhook timestamp (OSS):** every run-complete POST includes `X-Webhook-Timestamp: <unix-seconds>` (with or without HMAC). HMAC still signs the **raw JSON body only**. Mock receiver records the header; local-mvp asserts present and roughly now. **Timestamp replay window enforcement = paid later**
- **Run-complete webhook 1 retry (OSS):** on 5xx or network/timeout, retry the POST once after ~50ms (success on first try = no retry; 4xx do not retry). HMAC/timestamp/unsigned unchanged. Smoke unit-tests. **Exponential backoff / queues = paid**
- OpenAPI 3 (`openapi/runner.openapi.json`, `GET /openapi.json`) with Bearer scheme, `X-Request-Id` parameter/header, 202/429/401/404 on runs; documents `/health`, **`/ready`**, `/metrics`, `/v1/runs` GET+POST, `/v1/runs/{id}`, `/v1/runs/{id}/junit`, `/v1/runs/{id}/junit.xml`, `/v1/runs/junit.xml`, `/v1/runs/{id}/tap`, `/v1/runs/{id}/tap.txt`, `/v1/runs/tap.txt`, `/v1/runs/{id}/md`, `/v1/runs/{id}/report.md`, `/v1/runs/report.md`, `/v1/runs/{id}/annotations`, `/v1/runs/{id}/annotations.txt`, `/v1/runs/annotations.txt`, `/v1/runs/{id}/diff`, `/v1/runs/{id}/diff.md`, `/v1/runs/{id}/diff.html`, **`/v1/runs/{id}/cases`** (`listRunCases`), `/v1/check-runs`, `/v1/suites`, `/v1/suites/{name}`, **`/v1/config`** (`getConfig`) plus outbound `RunCompleteWebhook` (optional HMAC `X-Webhook-Signature`); local-mvp curl asserts paths
- **GET /ready (readiness):** 200 `{ok:true, queue}` when POST `/v1/runs` would still enqueue; 503 `{ok:false, reason:"queue_full"}` + `Retry-After` when at capacity (queued+running such that POST would 429). Snapshot only — does not consume a queue slot. Liveness `GET /health` stays 200 even when the queue is full. Compose/stack-demo healthchecks stay on `/health`. Isolated local-mvp (`--max-queue 1 --concurrency 1`): fill queue → `/health` 200 + `/ready` 503; after drain → `/ready` 200
- **Prometheus `GET /metrics`:** gauges `agent_ci_queue_depth`, `agent_ci_running` and counters `agent_ci_runs_completed_total`, `agent_ci_runs_failed_total` (Prometheus text 0.0.4). CORS: matching Origin GET includes ACAO (same as other GET). local-mvp curl after a completed run asserts metric names
- Dogfood A on C OpenAPI: `scripts/generate-runner-sdk.sh` / `make dogfood-a-c` → `bets/c-agent-ci/sdk/generated/` (gitignored; hooked from `local-mvp`)
- **Serve `--watch`:** `serve --watch` polls `fixtures/` max mtime every 400ms and logs `regenerated` (suiteCount + generation). `GET /v1/suites` still live-reads disk (new dirs appear without a cache). `GET /health` includes `watch: { generation }` (starts at 0; increments on change; omitted when `--watch` is off so stack-demo `/health` is unchanged). local-mvp isolated prove (mkdir a suite dir → list includes it + generation bumps → kill, no hang). Main serve / stack-demo unchanged (no `--watch`). Queue / webhook / CORS otherwise unchanged
- **Graceful SIGTERM/SIGINT drain:** always on. `/ready` **503** `{ok:false, reason:"shutting_down"}` (wins over healthy 200 / queue_full); `/health` stays **200** with `shuttingDown: true`. Stop new queue jobs. Drain default **5s** (`--drain-ms` / `SHUTDOWN_DRAIN_MS`, cap 30s); log `shutting down` then `exit`. Isolated local-mvp `kill -TERM` prove; smoke in-process `begin_shutdown`
- **JSON access logs (opt-in):** `serve --log-json` or env `LOG_FORMAT=json` (env wins if CLI omitted) — one stdout JSON line per completed app request `{ts,level:info,msg:http,service,method,path,status,durationMs,requestId}` (optional `bytesOut`/`remote`). Skips `/health` `/ready` `/metrics` and OPTIONS. Default **off**. Isolated local-mvp `--log-json` curl `X-Request-Id: test-log-1`; smoke formats the line
- **Finished-run history cap:** `--runs-max` / env `RUNS_MAX` default **1000** completed (`done`|`failed`|`error`) in-memory runs. Over cap drop oldest finished. Does **not** cap the in-flight queue. `0` = unlimited. `GET /v1/runs`, junit.xml aggregate, tap.txt aggregate, report.md aggregate, annotations.txt aggregate see retained history only; `GET /v1/runs/{id}` **404** if dropped. Webhook already fired at complete. smoke helper + in-process max=2 complete 3 → list 2, oldest 404; isolated local-mvp `--runs-max 2`; main demo default 1000 unchanged
- **GET /v1/config (redacted runtime config):** public GET like `/v1/suites` (no Bearer). Allowlist `{ok, queue.max/queued/running, failUnder, cors.origins, rateLimit.perMinute, runsMax, webhooks.hasUrl/hasSecret, suitesCount}`. **Never** webhook URL, secret, Bearer tokens, or fixture contents. CORS + `X-Request-Id`. OpenAPI `getConfig` / `RuntimeConfig`. smoke: 200 + planted secret absent; local-mvp curl 200 + isolated HMAC secret not leaked; stack-demo curl 200 + OpenAPI
- **GET /v1/suites/{name} (suite detail):** `{ok, id, name, path, caseCount, cases:[{name}]}` cassette names only (no fixture dump). Empty `cases:[]`. Unknown **404** `suite_not_found`. OpenAPI `getSuite` / `SuiteDetail`. smoke + local-mvp + stack-demo. No new deps
- **GET /v1/runs/{id}/cases (case inventory):** public GET like `/v1/runs/{id}`. `{ok, runId, status, count, cases:[{suite, name, status, durationMs?}]}`. Optional `?status=` (unknown → empty 200). Cap **500** original order + `truncated`. Empty / in-flight → **200** `cases:[]`. Unknown id → **404** `{ok:false, error:run_not_found}`. **Never** prompts, expected/actual, output, API keys, Authorization. OpenAPI `listRunCases` / `RunCases`. smoke helper + HTTP; local-mvp curl 200 + 404; stack-demo OpenAPI + 404. No new deps

#### D · ai-bom
- Directory scan → CycloneDX-like AI-BOM (models / prompts / MCP deps)
- **GitHub Actions SARIF example:** copy-paste [`examples/github-actions/ai-bom-sarif.yml`](./examples/github-actions/ai-bom-sarif.yml) (`python3 -m ai_bom scan examples/sample-app --policy policies/default.json --sarif ai-bom.sarif` + `github/codeql-action/upload-sarif@v3`; consumer must enable code scanning). Composite [`examples/github-actions/ai-bom-sarif/action.yml`](./examples/github-actions/ai-bom-sarif/action.yml). Not enabled on this repo (upload would fail without code scanning).
- Policy pack + `--strict` CI gates; bilingual `--evidence` DRAFT; `--sarif` SARIF 2.1.0
- SPDX / CycloneDX `licenses[]` on components from `package.json` / `pyproject.toml` / `requirements.txt` (NOASSERTION/UNKNOWN when unknown); evidence license summary counts; sample-app `package.json` MIT
- **Forbidden license gate:** `policies/default.json` `forbiddenLicenseIds` (GPL-3.0 / AGPL-3.0 / SSPL-1.0 + common variants); scan → `summary.forbiddenLicenses` policy hits; `--strict` exits 1; evidence + SARIF (`license/<id>`) include hits; local-mvp: MIT sample-app clean for licenses, temp GPL-3.0 `package.json` fails strict
- **License exceptions / waivers:** sidecar `.aibom-exceptions.json` (auto-detect like `.aibomignore`) + CLI `--exceptions PATH` / env `AI_BOM_EXCEPTIONS`. Named **component + license** waivers with required `reason` and optional `expires` (`YYYY-MM-DD` UTC). Match exact or glob on component. Applied → not a forbidden-license / `--strict` failure; `summary.waived[{component,license,reason}]`; expired still a hit + `summary.expiredExceptions`. Bad JSON / missing fields skip with warning (no crash; HTTP no 500). CycloneDX `aibom:waived`; SARIF note + `suppressions`. HTTP `?exceptions=skip` reconstructs un-waived. Does not replace path `.aibomignore`. smoke fixture + local-mvp temp sidecar scan/HTTP
- `.aibomignore` in scan root + CLI `--ignore "node_modules,dist,.git"` (simple gitignore-like prefix / `*` / directory-name patterns); local-mvp proves ignored vendor pickle vs root pickle
- **Local BOM HTTP server (OSS):** `serve --path … --port 8793` (Python stdlib `http.server`); `--host` default `127.0.0.1` (Compose `0.0.0.0`); `GET /health`, **`GET /ready`**, `GET /bom.json` (scan result), `GET /evidence.md`, `GET /` HTML summary (component count, license summary, policy hits), `GET /metrics` Prometheus text; **hosted inventory = paid later**; local-mvp start/curl/kill (assert MIT in `bom.json`)
- **GET /ready (readiness):** always **200** `{ok:true, service}` plus the same snapshot fields as `/health` (`componentCount`, `policyHits`, `licenses`). Stateless scan snapshot — no circuit/queue. CORS + `X-Request-Id` same as other GET. Liveness `GET /health` unchanged. Compose/stack-demo healthchecks stay on `/health`. OpenAPI `Ready` + `getReady`. local-mvp curl 200; stack-demo optional curl
- **CORS:** `serve --cors-origins` CSV or env `AI_BOM_CORS_ORIGINS` (`*` allowed); default empty/omit = no extra CORS headers. OPTIONS preflight: allowed Origin → 204 + ACAO; explicit-list miss → 403 `cors_denied`; disabled OPTIONS 404. GET includes ACAO when Origin matches. Default allow/expose headers include `X-Request-Id`. local-mvp isolated prove `origins: ["http://localhost:3000"]` (evil origin fails); existing HTML/bom.json/evidence/health + stack-demo curls unchanged (default deny)
- **CORS expose Retry-After**: default `Access-Control-Expose-Headers` includes `Retry-After` (keeps `X-Request-Id`); GET/OPTIONS local-mvp prove (case-insensitive); OpenAPI `CorsConfig` note; allowlist/deny unchanged
- **HTTP rate limit:** `--rate-limit` / env `RATE_LIMIT_PER_MINUTE` (alias `RATE_LIMIT_RPM`) default **120**; in-memory sliding window by client IP (`X-Forwarded-For` first hop, else socket); exceed → **429** `{ok:false, reason:"rate_limited"}` + `Retry-After`; skip `/health` `/ready` `/metrics`; `0` = unlimited; smoke helper N+1; isolated local-mvp `--rate-limit 2` (third `/bom.json` 429, `/health` 200); stack-demo default generous (no accidental 429)
- **`X-Request-Id`**: accept incoming header or generate UUID; echo on every response (incl 4xx/OPTIONS); CORS allow/expose `X-Request-Id`; local-mvp custom-id proof on `/health` and `/openapi.json`
- OpenAPI 3 (`openapi/bom.openapi.json`, `GET /openapi.json`) documents `/health`, **`/ready`**, `/bom.json`, `/evidence.md`, `/`, `/metrics` with `X-Request-Id` parameter/header and **403** CORS notes; local-mvp curl asserts paths
- **Prometheus `GET /metrics`:** snapshot gauges `ai_bom_component_count`, `ai_bom_policy_hits`, `ai_bom_forbidden_licenses` (Prometheus text 0.0.4). CORS: matching Origin GET includes ACAO (same as other GET). local-mvp curl asserts metric names
- Dogfood A on D OpenAPI: `scripts/generate-bom-sdk.sh` / `make dogfood-a-d` → `bets/d-ai-bom/sdk/generated/` (gitignored; hooked from `local-mvp`)
- **Policy-hit webhook (OSS):** `scan --webhook-url` or env `AI_BOM_WEBHOOK_URL`; on forbidden pattern hits or forbidden licenses, fire-and-forget POST JSON `{ok:false, policyHits, forbiddenLicenses, summary}` (short timeout; webhook errors never change exit code; **no POST on clean scan**); local-mvp mock receiver (`mock-webhook-receiver.py`) asserts `policyHits`; optional clean dir does not POST; OSS **1 retry** on 5xx/timeout; **exponential backoff / queues, key rotation / timestamp replay = paid later**
- **Policy-hit webhook HMAC (OSS):** optional `--webhook-secret` / env `AI_BOM_WEBHOOK_SECRET`. When set, POST includes `X-Webhook-Signature: sha256=<hex>` HMAC-SHA256 of the raw JSON body. Mock receiver optional `--secret` verify + `--headers-out`. Unsigned prove unchanged; isolated signed receiver in local-mvp. Simple HMAC is OSS; **key rotation / timestamp replay protection = paid later**
- **Outbound webhook timestamp (OSS):** every policy-hit POST includes `X-Webhook-Timestamp: <unix-seconds>` (with or without HMAC). HMAC still signs the **raw JSON body only**. Mock receiver records the header; local-mvp asserts present and roughly now. **Timestamp replay window enforcement = paid later**
- **Policy-hit webhook 1 retry (OSS):** on 5xx or network/timeout, retry the POST once after ~50ms (success on first try = no retry; 4xx do not retry). HMAC/timestamp/unsigned unchanged. Smoke unit-tests. **Exponential backoff / queues = paid**
- **Serve `--watch`:** `serve --path DIR --watch` polls directory walk max mtime every 500ms and rescans the snapshot used by `GET /bom.json`, `GET /health`, `GET /metrics`, `GET /` (and `/ready`; OpenAPI stays file-backed). Scan errors keep the previous snapshot; prints `regenerated` on success. local-mvp isolated temp-dir prove (curl `componentCount` → add a file → higher count → kill, no hang). Main serve / stack-demo unchanged (no `--watch`). CORS / OpenAPI / SARIF / scan CLI otherwise unchanged
- **Graceful SIGTERM/SIGINT drain:** always on. `/ready` **503** `{ok:false, reason:"shutting_down"}`; `/health` stays **200** with `shuttingDown: true`. Drain default **5s** (`--drain-ms` / `SHUTDOWN_DRAIN_MS`, cap 30s)
- **JSON access logs (opt-in):** `serve --log-json` or env `LOG_FORMAT=json` (env wins if CLI omitted) — one stdout JSON line per completed app request `{ts,level:info,msg:http,service,method,path,status,durationMs,requestId}`. Skips `/health` `/ready` `/metrics` and OPTIONS. Default **off**. Smoke formats the line
- **CycloneDX 1.5 + SPDX 2.3 export:** internal AI-BOM JSON stays default (`scan --format json` / `GET /bom.json` / `GET /v1/bom`). `scan --format cyclonedx|spdx` and `GET /v1/bom?format=cyclonedx|spdx` emit CycloneDX 1.5 (`bomFormat`+`specVersion`, component licenses, `properties` `aibom:policyHits`) and SPDX 2.3 (`spdxVersion`+`packages`+`documentNamespace`, `licenseConcluded`, `hasExtractedLicensingInfos` for forbidden-license hits). Unknown HTTP format → 400. OpenAPI `getBomV1` + `BomFormat`. local-mvp: CLI `--format spdx` + HTTP `?format=cyclonedx` 200; smoke fixture export. No new deps. ignore / license policy / webhook / watch / ready / metrics unchanged
- **HTTP SARIF 2.1.0:** `GET /v1/bom?format=sarif` and alias `GET /v1/bom.sarif` reuse CLI `to_sarif` (`version` `2.1.0` + `runs`; empty scan → empty `results` 200; `application/sarif+json`). `--format sarif` writes the same document to stdout/`--out` without breaking `--sarif PATH`. Unknown format still 400; default json / cyclonedx / spdx unchanged. OpenAPI `BomFormat` includes `sarif`; `getBomSarif`. local-mvp curl `?format=sarif` 200; smoke exporter + HTTP; stack-demo curl. GHA examples still use `--sarif PATH` (HTTP optional for serve/stack-demo). No new deps
- **CycloneDX 1.5 XML:** `scan --format cyclonedx-xml` (alias `cdx-xml`) and `GET /v1/bom?format=cyclonedx-xml` / `GET /v1/bom.xml` emit CycloneDX 1.5 XML (`<bom xmlns="http://cyclonedx.org/schema/bom/1.5" version="1" serialNumber=…>`; same components/licenses/`aibom:*` properties as JSON). Content-Type `application/vnd.cyclonedx+xml`. Names with `&` are escaped. Empty scan → valid empty `<components>` 200. `cyclonedx` stays JSON. Unknown format still 400 (`xml` is not a format). json/spdx/sarif unchanged. OpenAPI `BomFormat` includes `cyclonedx-xml`; `getBomXml`. smoke fixture + `&amp;`; local-mvp curl 200 + content-type xml. No new deps
- **SPDX 2.3 XML:** `scan --format spdx-xml` (alias `spdxxml`) and `GET /v1/bom?format=spdx-xml` / `GET /v1/bom.spdx.xml` emit SPDX 2.3 XML (`<SpdxDocument>` with `spdxVersion` SPDX-2.3, packages, `licenseConcluded`; same model as JSON). Content-Type `application/spdx+xml`. Names with `&` are escaped. Empty scan → valid document 200. `spdx` stays JSON. Unknown format still 400. json/cyclonedx/cyclonedx-xml/sarif unchanged. OpenAPI `BomFormat` includes `spdx-xml`; `getBomSpdxXml`. smoke fixture + `&amp;`; local-mvp curl 200 + content-type xml. No new deps
- **Markdown BOM summary (OSS):** `scan --format md` (alias `markdown`) and `GET /v1/bom?format=md` / `GET /v1/bom.md` emit a human/Slack Markdown summary (`# AI-BOM` + **components** / **policyHits** / **waived** + license / policy-hit / waived tables). Content-Type `text/markdown`. `|` escaped. Empty scan → heading + zeros **200**. **Not** another SBOM spec; never dumps file contents or secrets. json/cyclonedx/spdx/sarif/xml/exceptions unchanged. OpenAPI `BomFormat` includes `md`; `getBomMd`. smoke fixture `# ` + component name or license + policyHits number; local-mvp curl `/v1/bom.md` 200; stack-demo 200 or OpenAPI. No new deps
- **GitHub Actions workflow-command annotations (OSS):** `scan --format gha` (alias `annotations`) and `GET /v1/bom?format=gha` / `GET /v1/bom.gha.txt` emit `::error title=<component>::<license or rule>` for policy hits / forbidden licenses; waived hits → `::notice title=<component>::waived <reason>`. Content-Type `text/plain`. Clean scan → empty stdout / empty body **200** (no `::error`). `%` / CR / LF / `:` / `,` escaped like C `to_gha`. Does not require GitHub. json/cyclonedx/spdx/sarif/md/xml/exceptions unchanged. OpenAPI `BomFormat` includes `gha`; `getBomGha`. smoke fixture hit `::error` + waived-only notice + clean none; local-mvp `--format gha` + curl 200; GHA example README one-liner. No new deps
- **HTML BOM summary (OSS):** `scan --format html` and `GET /v1/bom?format=html` / `GET /v1/bom.html` emit a self-contained HTML summary (heading + component count + license table + policy hits / forbidden licenses). Content-Type `text/html; charset=utf-8`. No CDN / extra deps (`html.escape`). Policy hits / forbidden licenses use red fail styling. Names with `& < > "` escaped. Empty scan → heading + zeros **200**. `GET /` keeps the same formatter plus serve-index nav. **Not** another SBOM spec; CycloneDX/SPDX/SARIF/md/gha unchanged.
- **GET /v1/policy (active license/policy gate):** `{ok, forbiddenLicenseIds, forbiddenPatterns, exceptionsCount, ignoreFile}`. Real pack field names (`forbiddenLicenseIds` from `policies/default.json`; `exceptionsCount` like BOM metadata). Pattern **ids** only (no regex). Exception **count** only (no reasons). `ignoreFile` is whether scan-root `.aibomignore` exists (no file dump). Missing/unloaded policy → **200** empty lists. CORS + `X-Request-Id`. Optional CLI `ai-bom policy`. OpenAPI `getPolicy` + `PolicyGate`. smoke helper + HTTP (default pack includes **GPL-3.0**; no-policy serve empty 200); local-mvp curl 200; stack-demo curl 200. Scan / exceptions / formats unchanged. No new deps
- **GET /v1/config (redacted runtime knobs):** public GET like `/v1/policy` (no token). Allowlist `{ok, rateLimit.perMinute, cors.origins, watch, scanPathBase, hasPolicyFile, webhooks.hasUrl/hasSecret}`. `scanPathBase` is **basename only** (never a full host path). **Never** webhook URL, secret, full policy JSON (that is `/v1/policy`), or exception file contents. CORS + `X-Request-Id`. OpenAPI `getConfig` / `RuntimeConfig`. smoke: 200 + cors/rateLimit + planted secret absent; local-mvp curl 200 + isolated webhook not leaked; stack-demo curl 200 + OpenAPI. No new deps
- **GET /v1/components (lightweight inventory):** public GET from the last scan snapshot `{ok, count, components:[{name, version, license, path}]}`. `path` is relative to the scan root or a basename — **never** an absolute host path. Empty scan → **200** `{ok:true, count:0, components:[]}`. Optional `?license=` (case-insensitive exact). Cap **500** with `truncated:true`. Not CycloneDX/SPDX (exporters unchanged). CORS + `X-Request-Id`. OpenAPI `listComponents` / `ComponentInventory`. smoke: helper count matches sample-app + known name + HTTP 200; local-mvp curl 200 + OpenAPI path; stack-demo curl 200 + OpenAPI. No new deps
- **GET /v1/exceptions (redacted waiver inventory):** public GET `{ok, count, exceptions:[{component, license, expiresAt, expired}]}`. `count` is the full waiver count; array cap **500** original file order (`truncated:true` when more). Empty / no sidecar → **200** `{ok:true, count:0, exceptions:[]}`. Optional `?expired=true|false` (unknown → empty list 200). Optional short `reason` when already on the sidecar and not secret-like. **Never** sidecar path, full policy JSON, webhook URL/secret, API keys, or raw file dump. CORS + `X-Request-Id`. OpenAPI `listExceptions`. smoke helper + HTTP; local-mvp curl 200 + isolated sidecar; stack-demo curl 200 + OpenAPI. Scan / strict unchanged. No new deps
- Portfolio stack: Compose service `ai-bom` on **8793** (`Dockerfile` `python:3.12-alpine`, `serve --host 0.0.0.0 --port 8793`); `scripts/local-stack.sh` starts D + curls `/health`, **`/ready`**, `/bom.json` (MIT), `/openapi.json`, **`/v1/policy`**, **`/v1/config`**, **`/v1/components`**, **`/v1/exceptions`**, and `/metrics`

#### E · otel-ai-cost
- Token cost estimates from OTel spans (configurable price table); stdout table + self-contained HTML
- HTML report includes pure-SVG horizontal bar chart of cost by model (`byModel` totals; no external JS/CDN); existing table kept
- Redact/sample policy pack + `route` multi-sink sketch (kept vs dropped)
- Local budget thresholds (`policies/budget.json`, `report --budget` / `check-budget`; exit 1 lists breached limits)
- **Budget-breach webhook (OSS):** `report --budget … --webhook-url` or env `OTEL_AI_COST_WEBHOOK_URL`; on breach, fire-and-forget POST JSON `{ok:false, breaches, totalUsd}` (short timeout; webhook errors never change exit code; still exit 1 on breach; **no POST on pass**); local-mvp mock receiver (`mock-webhook-receiver.js`) asserts `totalUsd`/`breaches`; loose budget does not POST; OSS **1 retry** on 5xx/timeout; **exponential backoff / queues, key rotation / timestamp replay = paid later**
- **Budget-breach webhook HMAC (OSS):** optional `--webhook-secret` / env `OTEL_AI_COST_WEBHOOK_SECRET`. When set, POST includes `X-Webhook-Signature: sha256=<hex>` HMAC-SHA256 of the raw JSON body. Mock receiver optional `--secret` verify + `--headers-out`. Unsigned prove unchanged; isolated signed receiver in local-mvp. Simple HMAC is OSS; **key rotation / timestamp replay protection = paid later**
- **Outbound webhook timestamp (OSS):** every budget-breach POST includes `X-Webhook-Timestamp: <unix-seconds>` (with or without HMAC). HMAC still signs the **raw JSON body only**. Mock receiver records the header; local-mvp asserts present and roughly now. **Timestamp replay window enforcement = paid later**
- **Budget-breach webhook 1 retry (OSS):** on 5xx or network/timeout, retry the POST once after ~50ms (success on first try = no retry; 4xx do not retry). HMAC/timestamp/unsigned unchanged. Smoke unit-tests. **Exponential backoff / queues = paid**
- **UTC daily cost rollup:** example spans carry `timestamp` / `startTimeUnixNano`; `report --group-by day` prints per-UTC-day tables + TOTAL; HTML includes **by day (UTC)** section; `report --out out/daily.json --group-by day` writes structured `{groupBy,timezone,days[],totalUsd,…}`; local-mvp asserts
- **Finance CSV export:** `report --format csv|json|html` (default stdout table / `--html` / `--out` JSON unchanged). CSV columns `date,model,spanCount,usd,tenant` (`tenant` last so `date,model,spanCount,usd` greps still match) from UTC daily-by-model+tenant totals (`day` → `date`; same `usd` as `byDay[].byModel`) plus a TOTAL row; empty spans → header only. `GET /v1/costs.csv` and `GET /v1/costs?format=csv` return `text/csv` (200); `GET /v1/costs` default JSON same as `/report.json`; unknown format → 400 `bad_format`. CORS + `X-Request-Id`; access-logged (not skipped like `/metrics`). smoke fixture + empty 200; local-mvp CLI `--out` file + curl 200/`text/csv`
- **Markdown cost report (OSS):** `report --format md` stdout Markdown for Slack / email / GitHub Actions `$GITHUB_STEP_SUMMARY` (`# otel-ai-cost` + **totalUsd** / **spans** + `| model | usd | spans |` / `| tenant | usd | spans |`); `GET /v1/costs.md` + `GET /v1/costs?format=md` (`text/markdown; charset=utf-8`; `|` escaped; empty → heading + zeros 200). Same `byModel` / `byTenant` as JSON. csv/json/html unchanged. smoke fixture `# ` + `totalUsd` + `|` table + tenant acme; local-mvp curl 200 + markdown type; stack-demo OpenAPI or curl 200
- **GitHub Actions workflow-command annotations (OSS):** `report --format gha` (alias `--format annotations`) stdout `::error title=budget::totalUsd X > budget Y` (global `--budget` maxTotalUsd) and `::error title=tenant/<id>::usd X > budget Y` (each `budgetBreaches` tenant). No breach → empty stdout (no `::error`). `GET /v1/costs.gha.txt` + `GET /v1/costs?format=gha` (`text/plain; charset=utf-8`; empty 200). `%` / CR / LF / `:` / `,` escaped like C/D `to_gha`. Does not require GitHub. csv/json/html/md/OTLP/span-max unchanged. smoke fixture tiny tenant budget `::error` + tenant id; no-budget report no `::error`; local-mvp isolated `--tenant-budget acme=0.0001` CLI/HTTP; stack-demo OpenAPI path (empty, no tenant budget)
- **GitHub Actions budget-annotation example:** copy-paste [`examples/github-actions/otel-ai-cost-gha.yml`](./examples/github-actions/otel-ai-cost-gha.yml) (`node src/cli.js report --in examples/spans.json --format gha` + `$GITHUB_STEP_SUMMARY` md + `actions/upload-artifact@v4` `costs.md`; commented `--budget` / `--tenant-budget` for `::error`). Composite [`examples/github-actions/otel-ai-cost-gha/action.yml`](./examples/github-actions/otel-ai-cost-gha/action.yml). Parse-only `scripts/check-gha-examples.sh` on smoke. Not a required workflow in this repo.
- **Tenant cost attribution:** span attribute `tenant` (also `span.tenant`); missing/empty → `"_"`. JSON `byTenant: [{ tenant, usd, spanCount, byModel }]` on `report` / `toDailyJson` / `GET /report.json` / `GET /v1/costs`. CSV grain date+model+tenant. HTML small tenant table after the byModel chart (SVG unchanged). Prometheus tenant metric skipped (JSON/CSV first). smoke fixture `tenant=acme`; local-mvp curl JSON `byTenant` + CSV `tenant` column
- **Per-tenant budget (OSS):** `--tenant-budget acme=10,other=5` (CSV `k=v` / `k:v`) or env `OTEL_AI_COST_TENANT_BUDGETS` (alias `TENANT_BUDGETS`); JSON object / `.json` file also accepted; CLI wins; default none. After `byTenant`, `usd` > budget → `budgetBreaches: [{tenant,usd,budget}]` on report / daily JSON / `GET /report.json` / `GET /v1/costs` (empty array when unset; never 500). `_` not gated unless `_` is set explicitly. Global `--budget` (totalUsd) independent. Tenant-breach webhook reuses existing helper (body includes `tenant`; HMAC signs raw body; no tokens; missing tenant budgets → no extra webhook). smoke: acme spend > 0.01 with `acme=0.01` + in-process webhook `tenant=acme`; local-mvp isolated serve `--tenant-budget acme=0.0001` curl JSON
- **GET /v1/budgets (OSS):** configured global + tenant **thresholds** (not spend). `{ok:true, globalUsd: number|null, tenants:{acme:10,…}}`. `globalUsd` is `--budget` `maxTotalUsd` (missing → `null`); `tenants` is `--tenant-budget` / `OTEL_AI_COST_TENANT_BUDGETS` map (missing → `{}`). No secrets. CORS + `X-Request-Id`. smoke helper `acme=10` + default none; local-mvp isolated `--tenant-budget acme=10` curl 200; stack-demo empty 200
- **GET /v1/models (OSS):** pricing catalog (**rates**, not spend). `{ok:true, models:[{id, inputPerMTok, outputPerMTok, …}], defaultModel:null, pack:null}` from the loaded price table (built-in `DEFAULT_PRICES`: `gpt-4o` `inputPerMTok=2.5` / `outputPerMTok=10`, plus `gpt-4o-mini` / `claude-sonnet`). Field names match the table — do not invent rates. A single `usdPer1k` is used when that is all an entry has. Cached rates only when present. Empty/default still 200 with the built-in table. No secrets. CORS + `X-Request-Id`. Optional CLI `models` / `prices`. smoke helper + HTTP default pack id; local-mvp curl 200 + OpenAPI `getModels`; stack-demo curl 200 + OpenAPI path
- **GET /v1/config (redacted runtime config):** public GET like `/v1/budgets` (no token). Allowlist `{ok, spanCap, spansMax, rateLimit.perMinute, cors.origins, pack, hasGlobalBudget, tenantBudgetCount, webhooks.hasUrl/hasSecret}`. **Never** webhook URL, secret, ingest token, API keys, or price table dump (that is `/v1/models`). Dollar amounts stay on `/v1/budgets`. CORS + `X-Request-Id`. OpenAPI `getConfig` / `RuntimeConfig`. smoke: 200 + spanCap or cors + planted secret absent; local-mvp curl 200 + OpenAPI + isolated webhook secret not leaked; stack-demo curl 200 + OpenAPI
- **GET /v1/spans (span summaries):** public GET of recent ingested/file-loaded spans `{ok:true, count, spans:[{id, model, tenant, inputTokens, outputTokens, usd, ts}]}`. Allowlist only — **never** prompt/completion/input/output text, API keys, or Authorization. Newest first. Cap **100**; `count` is the full retained ring size; `truncated: true` when more. Empty → **200** `{ok:true, count:0, spans:[]}`. CORS + `X-Request-Id`. OpenAPI `listSpans` / `SpanList`. smoke: empty 200; after load/ingest count>=1 + model present; planted `SECRET_PROMPT` absent; local-mvp curl 200 after default spans file + OpenAPI; stack-demo fixture 200 + OpenAPI
- **GET /v1/tenants (tenant spend inventory):** public GET of current in-memory store `{ok:true, count, tenants:[{id, spanCount, usd}]}`. Missing/empty tenant → `_`. Sort highest `usd` first, then id. Cap **100**; `count` is the full tenant count; `truncated: true` when more. Optional `budgetUsd` only when that tenant has a configured budget (`GET /v1/budgets`.tenants). Optional `?tenant=` exact filter (unknown → empty 200). **Never** prompts, completions, API keys, Authorization, webhook URL/secret, ingest token, or the price catalog. Empty → **200** `{ok:true, count:0, tenants:[]}`. CORS + `X-Request-Id`. OpenAPI `listTenants` / `TenantList`. smoke: empty 200; after load/ingest count>=1 + known tenant or `_`; planted `SECRET_PROMPT` absent; helper 101→100 + truncated; local-mvp curl 200 after default spans file + OpenAPI; stack-demo fixture 200 + OpenAPI
- **Local report server (OSS):** `serve --port 8792 --in spans.json` (Node stdlib `http` only); `--host` default `127.0.0.1` (Compose `0.0.0.0`); `GET /health`, **`GET /ready`**, `GET /` self-contained HTML + SVG chart (+ UTC daily when `--group-by day` default/flag), `GET /report.json` structured JSON with `totalUsd`, **`GET /v1/costs.csv`**, `GET /metrics` Prometheus text; **hosted dashboard = paid later**; local-mvp start/curl/kill
- **GET /ready (readiness):** always **200** `{ok:true, service}` plus the same snapshot fields as `/health` (`spanCount`, `totalUsd`). Stateless cost snapshot — no circuit/queue. CORS + `X-Request-Id` same as other GET. Liveness `GET /health` unchanged. Compose/stack-demo healthchecks stay on `/health`. OpenAPI `Ready` + `getReady`. local-mvp curl 200; stack-demo optional curl
- **CORS:** `serve --cors-origins` CSV or env `OTEL_AI_COST_CORS_ORIGINS` (`*` allowed); default empty/omit = no extra CORS headers. OPTIONS preflight: allowed Origin → 204 + ACAO; explicit-list miss → 403 `cors_denied`; disabled OPTIONS 404. GET includes ACAO when Origin matches. Default allow/expose headers include `X-Request-Id`. local-mvp isolated prove `origins: ["http://localhost:3000"]` (evil origin fails); existing HTML/json/health + stack-demo curls unchanged (default deny)
- **CORS expose Retry-After**: default `Access-Control-Expose-Headers` includes `Retry-After` (keeps `X-Request-Id`); GET/OPTIONS local-mvp prove (case-insensitive); OpenAPI `CorsConfig` note; allowlist/deny unchanged
- **HTTP rate limit:** `--rate-limit` / env `RATE_LIMIT_PER_MINUTE` (alias `RATE_LIMIT_RPM`) default **120**; in-memory sliding window by client IP (`X-Forwarded-For` first hop, else socket); exceed → **429** `{ok:false, reason:"rate_limited"}` + `Retry-After`; skip `/health` `/ready` `/metrics`; `0` = unlimited; smoke helper N+1; isolated local-mvp `--rate-limit 2` (third `/report.json` 429, `/health` 200); stack-demo default generous (no accidental 429)
- **`X-Request-Id`**: accept incoming header or generate UUID; echo on every response (incl 4xx/OPTIONS); CORS allow/expose `X-Request-Id`; local-mvp custom-id proof on `/health` and `/openapi.json`
- OpenAPI 3 (`openapi/cost.openapi.json`, `GET /openapi.json`) documents `/health`, **`/ready`**, `/`, `/report.json`, **`/v1/costs.csv`** (`getCostsCsv`), **`/v1/costs.md`** (`getCostsMd`), **`/v1/costs.gha.txt`** (`getCostsGha`), **`/v1/costs`** (`getCosts`, `?format=`), **`/v1/budgets`** (`getBudgets`), **`/v1/models`** (`getModels`), **`/v1/config`** (`getConfig`), **`/v1/spans`** (`listSpans`), **`/v1/tenants`** (`listTenants`), `/metrics`, **`POST /v1/traces`** (`postTraces`) with `X-Request-Id` parameter/header and **403** CORS notes; local-mvp curl asserts paths
- **Prometheus `GET /metrics`:** snapshot gauges `otel_ai_cost_total_usd`, `otel_ai_cost_by_model_usd{model}` and counter `otel_ai_cost_span_count` (Prometheus text 0.0.4). CORS: matching Origin GET includes ACAO (same as other GET). local-mvp curl asserts metric names
- **Serve `--watch`:** `serve --in spans.json --watch` polls spans mtime every 200ms (`fs.stat` + `setInterval`) and reloads the snapshot used by `GET /`, `GET /report.json`, `GET /v1/costs.csv`, `GET /v1/costs.md`, `GET /metrics`, `GET /health` (and `/ready`; OpenAPI stays file-backed). Parse errors keep the previous snapshot; prints `regenerated` on success. local-mvp isolated temp-copy prove (curl `totalUsd` → append span → higher `totalUsd`/`spanCount` → kill, no hang). Main serve / stack-demo unchanged (no `--watch`). CORS / OpenAPI / metrics handlers otherwise unchanged
- **OTLP JSON ingest (live FinOps):** `POST /v1/traces` (alias `POST /v1/otlp/v1/traces`) accepts simplified OTLP `ExportTraceServiceRequest` JSON (`resourceSpans[].scopeSpans[].spans[]`, attributes map or `{key,value:{stringValue|intValue}}`) or E's existing flat `{spans:[]}` / array. Merges into the in-memory store so `/v1/costs` / HTML / CSV / metrics include them. Auth none by default; optional `--ingest-token` / `INGEST_TOKEN` → require `Authorization: Bearer` (**401** missing/wrong; token never echoed). Default off so stack-demo never 401s. Bad JSON → **400** `{error:"bad_json"}`; empty → **200** `{ok:true, accepted:0}`; body > **1048576** → **413** `{error:"payload_too_large"}` before parse (B-style). Ingest is rate-limited like other app routes. JSON only (no protobuf). smoke: OTLP-shaped span `tenant=acme` `model=gpt-4o` → `/v1/costs` byTenant acme / byModel gpt-4o; unsigned 200; token 401; 413. local-mvp isolated port: POST then CSV new row; unsigned 200; `--ingest-token` 401 without bearer. Main serve / stack-demo file snapshot unchanged
- **In-memory span cap:** live store retains at most **50000** spans (default; `--span-max` / `SPAN_MAX`). Over cap, drop oldest. Costs / CSV / HTML / metrics recompute from the retained window. `0` = unlimited (**dangerous**). Ingest still **200**; `accepted` is how many were parsed, not how many fit. File `--watch` reload replaces the store then caps. smoke: in-process max=2 ingest 3 → retained 2, oldest gone, costs `spanCount` 2; isolated local-mvp `--span-max 2` POST 3 tiny spans then `GET /v1/costs` `spanCount=2`; main demo file 6 spans still 6 (under cap)
- **Graceful SIGTERM/SIGINT drain:** always on. `/ready` **503** `{ok:false, reason:"shutting_down"}`; `/health` stays **200** with `shuttingDown: true`. Drain default **5s** (`--drain-ms` / `SHUTDOWN_DRAIN_MS`, cap 30s)
- **JSON access logs (opt-in):** `serve --log-json` or env `LOG_FORMAT=json` (env wins if CLI omitted) — one stdout JSON line per completed app request `{ts,level:info,msg:http,service,method,path,status,durationMs,requestId}`. Skips `/health` `/ready` `/metrics` and OPTIONS. Default **off**. Smoke formats the line
- Dogfood A on E OpenAPI: `scripts/generate-cost-sdk.sh` / `make dogfood-a-e` → `bets/e-otel-ai-cost/sdk/generated/` (gitignored; hooked from `local-mvp`)
- Portfolio stack: Compose service `otel-ai-cost` on **8792** (`Dockerfile` `node:20-alpine`, `node src/cli.js serve --host 0.0.0.0 --port 8792 --in examples/spans.json`); `scripts/local-stack.sh` starts E + curls `/health`, **`/ready`**, `/openapi.json`, `/v1/costs.csv`, `/v1/costs.md`, `/v1/costs.gha.txt` (empty — no tenant budget), **`/v1/budgets`** (empty `{ok:true, globalUsd:null, tenants:{}}`), **`/v1/models`** (built-in catalog `{ok:true, models:[…]}`), **`/v1/config`** (redacted knobs; no secrets), **`/v1/spans`** (fixture summaries; no prompts), **`/v1/tenants`** (fixture spend rollup; no prompts), and `/metrics`

#### F · cn-work-agent
- On-prem Feishu / DingTalk / WeCom webhook mocks → shared intent → local tools (stdlib only)
- Per-platform verify (401 on bad); config-driven serve; intranet demo + `demo-ask-reply.sh`
- Simple approval flow: intent triggers → `data/approvals.jsonl`; `GET /approvals` + `GET`/`POST /approvals/{id}/decide` (audit on decide)
- **Native IM approval cards:** `GET /v1/approvals/{id}/card?platform=feishu|dingtalk|wecom` returns the JSON body F would POST to that IM (Feishu interactive `header`/`elements` + Approve/Reject buttons; DingTalk `msgtype=actionCard`; WeCom `msgtype=textcard`). Buttons use existing `/approvals/{id}/decide` URLs or dummy `https://example.invalid/approve?id=`. No tokens/HMAC/`sk-` in the payload. Create ack includes `card`. Unknown id → 404. smoke fake approval (all 3 platforms); local-mvp curl after create; stack-demo OpenAPI path + 404
- **Approval audit CSV export:** `export --format csv|json` (default csv stdout; `--out` file). Columns `id,platform,status,createdAt,decidedAt,reason` from JSONL (`created_at` → `createdAt`; `updated_at` → `decidedAt` when approved/rejected). Includes TTL auto-rejects (`reason=expired`). Does **not** include message `text`, `note`, tokens, or webhook HMAC. Empty → header only. `GET /v1/approvals.csv` and `GET /v1/approvals?format=csv` return `text/csv` (200); `GET /v1/approvals` default JSON same as `/approvals`; unknown format → 400 `bad_format`. CORS + `X-Request-Id`; access-logged (not skipped like `/metrics`). smoke approved+expired rows; local-mvp curl after decide + empty header on CORS isolate / stack-demo
- **Approval Markdown list:** `export --format md` stdout GFM table for Feishu/WeCom docs (`# Approvals` + `| id | platform | status | createdAt | decidedAt | reason |`). `GET /v1/approvals.md` and `GET /v1/approvals?format=md` (`text/markdown; charset=utf-8`; `|` escaped; empty → heading + header row only, 200). Same columns as CSV; no `text`/tokens/HMAC. Cards / inbound HMAC / approvals-max / CSV unchanged. smoke one approved + one rejected (`# `, `|`, statuses; no token); local-mvp create/decide curl md 200; stack-demo empty md 200 or OpenAPI
- **Approval HTML list:** `export --format html` stdout self-contained HTML (stdlib `html.escape`, inline CSS, no CDN). `GET /v1/approvals.html` and `GET /v1/approvals?format=html` (`text/html; charset=utf-8`). Columns id/platform/status/title/created/decided/reason (CSV/MD plus title from `text`). Pending vs decided/expired rows visually distinct. Empty → heading + “no approvals”, 200. CORS + `X-Request-Id`. CSV/md/cards/verify unchanged. smoke `<table` + escaped title (`&lt;script&gt;`); empty 200; local-mvp curl 200 `text/html` after create; stack-demo empty `/v1/approvals.html` 200 + OpenAPI `getApprovalsHtml` / `format=html`
- Webhook **rate limit**: `RATE_LIMIT_PER_MINUTE` default 60 (optional per-platform env/config); in-memory sliding window by IP + platform path; exceed → `429` JSON + `Retry-After`; local-mvp proves on isolated port with limit=2
- Approval **TTL auto-reject**: `APPROVAL_TTL_SECONDS` / `approval_ttl_seconds` (default 86400); `expire_due()` on list/get/decide + periodic serve loop marks pending → `rejected` with `reason=expired`; CLI `expire-approvals`; local-mvp proves TTL=1 → sleep 2 → cannot approve
- **CORS**: env `CORS_ORIGINS` CSV or config `cors.origins` (`*` allowed); optional `--cors-origins`. Default empty/omit = no extra CORS headers. OPTIONS preflight: allowed Origin → 204 + ACAO; explicit-list miss → 403 `cors_denied`. GET/POST include ACAO when Origin matches. Default allow/expose headers include `X-Request-Id`. local-mvp isolated prove `origins: ["http://localhost:3000"]` (evil origin fails); existing webhook/auth/approvals/rate-limit/TTL curls unchanged
- **CORS expose Retry-After**: default `Access-Control-Expose-Headers` includes `Retry-After` (keeps `X-Request-Id`); GET/POST/OPTIONS local-mvp prove (case-insensitive); OpenAPI `CorsConfig` note; allowlist/deny unchanged
- **`X-Request-Id`**: accept incoming header or generate UUID; echo on every response (incl 4xx/OPTIONS/429); store `requestId` on approval records and audit JSONL; CORS allow/expose `X-Request-Id`; local-mvp custom-id proof (response header + stored approval)
- OpenAPI 3 (`openapi/agent.openapi.json`, `GET /openapi.json`) documents `/health`, **`/ready`**, **`GET /v1/platforms`** (`getPlatforms`), **`GET /v1/config`** (`getConfig`), `/metrics`, `/webhook/feishu`, `/webhook/dingtalk`, `/webhook/wecom`, `GET /approvals`, `GET /approvals/{id}`, `GET`/`POST /approvals/{id}/decide` (POST optional inbound `X-Callback-Signature` **401**), **`GET /v1/approvals.csv`** (`getApprovalsCsv`), **`GET /v1/approvals.md`** (`getApprovalsMd`), **`GET /v1/approvals.html`** (`getApprovalsHtml`), **`GET /v1/approvals`** (`getApprovals`, `?format=`), **`GET /v1/approvals/{id}/card`** (`getApprovalCard`) with `X-Request-Id`, webhook **401/429**, **403** CORS notes, and outbound `ApprovalDecisionWebhook` (optional HMAC `X-Webhook-Signature`); local-mvp curl asserts webhook, approvals, `/ready`, and `/metrics` paths
- **GET /ready (readiness):** always **200** `{ok:true, service}` plus the same snapshot fields as `/health` (`platforms` / `enabled` / `rate_limit_per_minute` / `approval_ttl_seconds`). Stateless HTTP — no circuit/queue; not rate-limited. CORS + `X-Request-Id` same as other GET. Liveness `GET /health` unchanged. Compose/stack-demo healthchecks stay on `/health`. OpenAPI `Ready` + `getReady`. local-mvp curl 200; stack-demo optional curl
- **Prometheus `GET /metrics`:** gauge `cn_work_agent_approvals_pending` (JSONL pending; scrape does not run expire_due) and counters `cn_work_agent_approvals_decided_total`, `cn_work_agent_webhooks_total` (Prometheus text 0.0.4). CORS: matching Origin GET includes ACAO (same as other GET). local-mvp curl after create/decide asserts metric names
- Dogfood A on F OpenAPI: `scripts/generate-agent-sdk.sh` / `make dogfood-a-f` → `bets/f-cn-work-agent/sdk/generated/` (gitignored; hooked from `local-mvp`)
- **Approval-decision webhook (OSS):** `serve --webhook-url` or env `APPROVAL_WEBHOOK_URL` (config `approval_webhook_url`); when an approval is decided (`approve`/`reject`) or TTL-expired, fire-and-forget POST JSON `{id,status,decision,reason,requestId}` (short timeout; webhook errors never fail decide/expire; **no POST on create**); local-mvp isolated mock receiver (`mock-webhook-receiver.py`) asserts payload; OSS **1 retry** on 5xx/timeout; **exponential backoff / queues, key rotation / timestamp replay = paid later**
- **Approval-decision webhook HMAC (OSS):** optional `--webhook-secret` / env `APPROVAL_WEBHOOK_SECRET` (config `approval_webhook_secret`). When set, POST includes `X-Webhook-Signature: sha256=<hex>` HMAC-SHA256 of the raw JSON body. Mock receiver optional `--secret` verify + `--headers-out`. Unsigned prove unchanged; isolated signed receiver in local-mvp. Simple HMAC is OSS; **key rotation / timestamp replay protection = paid later**
- **Outbound webhook timestamp (OSS):** every approval-decision POST includes `X-Webhook-Timestamp: <unix-seconds>` (with or without HMAC). HMAC still signs the **raw JSON body only**. Mock receiver records the header; local-mvp asserts present and roughly now. **Timestamp replay window enforcement = paid later**
- **Approval-decision webhook 1 retry (OSS):** on 5xx or network/timeout, retry the POST once after ~50ms (success on first try = no retry; 4xx do not retry). HMAC/timestamp/unsigned unchanged. Smoke unit-tests. **Exponential backoff / queues = paid**
- **Serve `--watch`:** `serve --config config.json --watch` polls config mtime every 300ms and reloads CORS origins, approval TTL, webhook url/secret, and rate limits from file. Env still wins if already set (CLI flags still win when provided). Parse errors keep the previous settings; prints `regenerated` on success. local-mvp isolated copy prove (curl `/health` TTL → change `approval_ttl_seconds` → health shows new TTL → kill, no hang). Main serve / stack-demo unchanged (no `--watch`). Multi-IM / rate-limit / HMAC otherwise unchanged
- **Graceful SIGTERM/SIGINT drain:** always on. `/ready` **503** `{ok:false, reason:"shutting_down"}`; `/health` stays **200** with `shuttingDown: true`. Drain default **5s** (`--drain-ms` / `SHUTDOWN_DRAIN_MS`, cap 30s); watch/expire threads stop immediately
- **JSON access logs (opt-in):** `serve --log-json` or env `LOG_FORMAT=json` (env wins if CLI omitted) — one stdout JSON line per completed app request `{ts,level:info,msg:http,service,method,path,status,durationMs,requestId}`. Skips `/health` `/ready` `/metrics` and OPTIONS. Default **off**. Smoke formats the line
- **Inbound IM callback HMAC (OSS):** optional per-platform `callbackSecret` (`feishu.callbackSecret` / `platforms.feishu.callbackSecret` / aliases `verificationToken` / `appSecret`; env `FEISHU_CALLBACK_SECRET` / `DINGTALK_CALLBACK_SECRET` / `WECOM_CALLBACK_SECRET` / `APPROVAL_CALLBACK_SECRET`). Default **off** so unsigned local-mvp POST decide stays **200**. When set: **POST** `/approvals/{id}/decide` requires `X-Callback-Signature: sha256=<hex>` HMAC-SHA256 of the raw body (same style as outbound `X-Webhook-Signature`; timing-safe compare); optional `X-Callback-Timestamp` (skew > 300s → 401 `timestamp_skew`). Missing/bad signature → **401** (secret never leaked). **GET** decide stays unsigned for IM card buttons (production should POST + signature; Feishu/DingTalk/WeCom adapters can copy this header later). Does not reuse IM `verify_token` / `encrypt_key` / DingTalk `secret`. smoke helper: valid HMAC 200 / bad 401 / no secret unsigned POST 200; isolated local-mvp config secret: good POST 200, bad/unsigned/skew 401, GET 200; main demo unchanged. Cards / CSV / TTL / rate-limit / outbound HMAC unchanged
- **Decided-approvals cap:** `--approvals-max` / env `APPROVALS_MAX` (config `approvals_max`) default **2000** decided (`approved`/`rejected`/`expired`) JSONL rows. Over cap drop oldest decided by `created_at`. Pending within TTL are never dropped. `0` = unlimited. `GET /approvals`, `GET /v1/approvals.csv`, `GET /v1/approvals.md`, `GET /v1/approvals.html`, and cards see retained rows only; `GET /approvals/{id}` **404** if dropped. Webhook already fired at decide/expire. smoke helper + max=2 with 3 decided → 2 retained, oldest get None/404; isolated local-mvp `--approvals-max 2` create+decide 3 → list/CSV 2; main demo default 2000 unchanged. Cards / inbound HMAC / TTL / rate-limit / watch unchanged
- **`GET /v1/platforms` IM inventory:** `{ok, count, platforms:[{id, enabled, hasCallbackSecret}]}` for feishu / dingtalk / wecom plus extras already in config `platforms`. **Never** tokens, `callbackSecret`, or encrypt keys. CORS + `X-Request-Id`. Not rate-limited (same as other GETs besides `/webhook/*`). `/health` still lists id strings only. Optional CLI `platforms`. OpenAPI `getPlatforms` / `PlatformList`. smoke helper + in-process HTTP (ids, no secret strings); local-mvp curl 200 + ids; stack-demo curl 200
- **`GET /v1/config` redacted runtime config:** public GET (no admin token; mirrors B `GET /admin/config` allowlist). `{ok, approvalTtlSec, rateLimit:{perMinute}, cors:{origins}, approvalsMax, webhooks:{hasUrl,hasSecret}, platforms:[{id,enabled,hasCallbackSecret}]}`. **Never** webhook URL (query tokens), webhook secret, `callbackSecret`, `FEISHU_*` tokens, or `Authorization`. CORS + `X-Request-Id`. Not rate-limited. Optional CLI `config`. OpenAPI `getConfig` / `RuntimeConfig`. smoke helper + in-process HTTP (200; platforms or ttl; planted secret would fail); local-mvp curl 200 + OpenAPI + no secret strings from env; stack-demo curl 200 + OpenAPI
- **`GET /v1/approvals?status=`:** `pending` / `approved` / `rejected` / `expired` on JSON + CSV/MD/HTML (shared list helper). Unknown/empty → **200** empty list (not 400). Omit → unfiltered. `expired` matches TTL auto-rejects (`reason=expired`). OpenAPI enum on `getApprovals` / csv / md / html. smoke + local-mvp + stack-demo. CLI `export --status` same tokens


#### Portfolio / ops
- OSS hygiene: Apache-2.0 [`NOTICE`](./NOTICE) (copyright 2026, project `oss-cash-lab`) + [`.editorconfig`](./.editorconfig) (`root = true`, UTF-8/LF, indent 2 for js/json/yml/md, 4 for py, tab for Makefile/Go) + [`.gitattributes`](./.gitattributes) `* text=auto eol=lf` + [`SECURITY.md`](./SECURITY.md) + [`CODE_OF_CONDUCT.md`](./CODE_OF_CONDUCT.md). Smoke prove via `scripts/check-oss-hygiene.sh` / `make check-oss-hygiene` (files exist; NOTICE names the project; editorconfig has `root = true` + `indent_size`; SECURITY.md mentions Security Advisories / vulnerability + Apache/portfolio; CODE_OF_CONDUCT.md mentions Contributor Covenant / 行为准则 + enforcement / 执行). **Does not restyle the tree.**
- Stack demo: `scripts/local-stack.sh` curl proofs use `grep -q ... <<<"$var"` (avoid `echo | grep -q` SIGPIPE/141 under `pipefail` on large OpenAPI bodies)
- Helm chart: `deploy/helm/oss-cash-lab/` (thin Deployment+Service for B–F; same `ghcr.io/wozqhl/<bet>:dev` images and ports as `deploy/k8s/`; optional NetworkPolicy via `networkPolicy.enabled` default false; optional PDB via `pdb.enabled` default false so replicaCount 1 does not block drains; optional HPA via `hpa.enabled` default false so replicaCount 1 is not managed without metrics-server; optional Ingress via `ingress.enabled` default false so install does not require a controller; optional LimitRange via `limitRange.enabled` default **true** so omitted container resources still get requests/limits; optional ResourceQuota via `resourceQuota.enabled` default **false** so a thin install into a shared ns does not fight existing quotas; optional securityContext via `securityContext.enabled` default **true** (restricted-ish PSS on B–F Deployments); **no** ServiceMonitor/PrometheusRule CRDs). `templates/NOTES.txt` after `helm install` (port-forward each ClusterIP `svc/<bet> <cli-port>:80` then curl `/health` + `/ready`; five `*.oss-cash-lab.local` hosts + `/etc/hosts` when `ingress.enabled`; placeholder images `ghcr.io/wozqhl/<bet>:dev` not published). `scripts/check-helm.sh` parse-only on `make smoke` / `make check-helm` (`helm template` if helm on PATH — skip like docker; NOTES.txt must exist with port-forward + `/health`; rendered NOTES contain those strings; `--set pdb.enabled=true` must render 5 PDBs; `--set hpa.enabled=true` must render 5 HPAs; `--set ingress.enabled=true` must render 1 Ingress with 5 hosts and NOTES lists those hosts; default template renders 1 LimitRange and 0 ResourceQuota; `--set limitRange.enabled=false` renders 0 LimitRange; `--set resourceQuota.enabled=true` renders 1 ResourceQuota; default template renders securityContext fields; `--set securityContext.enabled=false` omits them). Kustomize remains the default apply path. No cluster.
- Kubernetes NetworkPolicy: `deploy/k8s/networkpolicy.yaml` (one `NetworkPolicy` per B–F bet; `networking.k8s.io/v1`; **listed in** `kustomization.yaml`). `policyTypes` Ingress+Egress. Ingress TCP named port `http` from same-namespace (`podSelector: {}`), Prometheus scrape (`app.kubernetes.io/name: prometheus` any namespace), and `kube-system`. Egress DNS UDP/TCP 53 + HTTPS 443 (webhooks/upstreams — do not empty-deny egress). Docs warn: kubelet probes come from the node network; CNI must implement NetworkPolicy. Parse-only `scripts/check-k8s.py` (5 policies, Ingress+Egress, B–F podSelectors, egress 53 or 443). No cluster.
- Kubernetes PodDisruptionBudget: `deploy/k8s/pdb.yaml` (one `PodDisruptionBudget` per B–F bet; `policy/v1`; **listed in** `kustomization.yaml`). Selector `app.oss-cash-lab.dev/<bet>: "true"`. **`maxUnavailable: 1`** so replica=1 clusters can still drain (`minAvailable: 1` + replicas 1 **blocks** node drain). HA: bump replicas to ≥2 then use `minAvailable: 1`. Optional Helm `pdb.enabled` default **false** (`templates/pdb.yaml`; `maxUnavailable: 1` when on). Parse-only `scripts/check-k8s.py` (5 PDBs, B–F selectors, maxUnavailable or minAvailable set). No cluster.
- Kubernetes HorizontalPodAutoscaler: `deploy/k8s/hpa.yaml` (one `HorizontalPodAutoscaler` per B–F bet; `autoscaling/v2`; **listed in** `kustomization.yaml`). `scaleTargetRef` the Deployment; **`minReplicas: 1`** / **`maxReplicas: 4`**; CPU `averageUtilization: 70`. Does **not** change Deployment `replicas: 1`. **Needs metrics-server** (`metrics.k8s.io`) to scale — without it HPA objects sit idle. Optional Helm `hpa.enabled` default **false** (`templates/hpa.yaml`; same min/max/CPU when on). Parse-only `scripts/check-k8s.py` (5 HPAs, min≤max, CPU target, B–F scaleTargetRef). No cluster.
- Kubernetes Ingress: `deploy/k8s/ingress.yaml` (one `Ingress`; `networking.k8s.io/v1`; **listed in** `kustomization.yaml`). Five host rules Prefix `/` so backends need no path strip: `gateway.oss-cash-lab.local` → `b-mcp-gateway`, `ci.oss-cash-lab.local` → `c-agent-ci`, `bom.oss-cash-lab.local` → `d-ai-bom`, `cost.oss-cash-lab.local` → `e-otel-ai-cost`, `agent.oss-cash-lab.local` → `f-cn-work-agent`. `ingressClassName: nginx` (change for Traefik/Contour/etc.). **Needs an ingress controller** (ingress-nginx or similar) — without it the object sits idle. **TLS omitted** (local; no Secrets). Document `/etc/hosts`. Optional Helm `ingress.enabled` default **false** (`templates/ingress.yaml`; host/class in values). Parse-only `scripts/check-k8s.py` (1 Ingress, 5 hosts or 5 rules, B–F Service backends, class nginx). No cluster.
- Kubernetes LimitRange: `deploy/k8s/limitrange.yaml` (one namespace `LimitRange`; `v1`; **listed in** `kustomization.yaml`). `type: Container`. `min` 25m/32Mi, `defaultRequest` 50m/64Mi (current Deployment requests), `default` 500m/256Mi (current limits), `max` 1/512Mi. Fills **omitted** container requests/limits so HPA has a request denominator and noisy-neighbor pods cannot omit limits. Does **not** change Deployments that already set `resources`. Optional Helm `limitRange.enabled` default **true** (`templates/limitrange.yaml`; `--set limitRange.enabled=false` skips). Parse-only `scripts/check-k8s.py` (1 LimitRange, type Container, `defaultRequest.cpu`). No cluster.
- Kubernetes ResourceQuota: `deploy/k8s/resourcequota.yaml` (one namespace `ResourceQuota`; `v1`; **listed in** `kustomization.yaml`). Hard caps sized for 5 B–F × HPA `maxReplicas: 4` plus headroom: `pods` 24, `requests.cpu` 2, `requests.memory` 2Gi, `limits.cpu` 12, `limits.memory` 6Gi, `services` 10. Floor **≥** replica=1 Deployment requests so apply does not block the demo. Pairs with LimitRange + HPA. Does **not** change Deployment resource blocks or HPA maxReplicas. Optional Helm `resourceQuota.enabled` default **false** (`templates/resourcequota.yaml`; `--set resourceQuota.enabled=true` renders 1) so a thin install into a shared ns does not fight existing quotas. Parse-only `scripts/check-k8s.py` (1 ResourceQuota, `hard.pods` set). No cluster.
- Kubernetes securityContext: baked into each B–F Deployment in `deploy/k8s/` (pod `runAsNonRoot` + numeric `runAsUser` matching Dockerfile USER — **1000** for B/E `node:20-alpine` `USER node`, **65532** for C/D/F `USER 65532`; `seccompProfile.type: RuntimeDefault`; container `allowPrivilegeEscalation: false`, `readOnlyRootFilesystem: true`, `capabilities.drop: [ALL]`, `runAsNonRoot: true`; `emptyDir` `/tmp` on all, `/app/data` on B/C/F). Optional Helm `securityContext.enabled` default **true** (`templates/bets.yaml`; `--set securityContext.enabled=false` omits). Parse-only `scripts/check-k8s.py` + `scripts/check-helm.py`. No cluster. Does **not** change ports, probes, resources, or B `/admin/webhooks`.
- Kubernetes: `deploy/k8s/` Deployment+Service for B/C/D/E/F (placeholder `ghcr.io/wozqhl/<bet>:dev` — **images not published**; liveness `/health`, readiness `/ready`, `terminationGracePeriodSeconds: 10`; ClusterIP 80 named `http` to B 8787 / C 8791 / D 8793 / E 8792 / F 8790); `scripts/check-k8s.sh` parse-only on `make smoke` (PyYAML or stdlib subset; no cluster / Helm)
- Prometheus Operator ServiceMonitors: `deploy/k8s/servicemonitor.yaml` (one `ServiceMonitor` per B–F bet; scrape `port: http` `path: /metrics` `interval: 30s`; `namespaceSelector.any: true`). Requires CRDs `monitoring.coreos.com/v1` — **not applied in smoke**; parse-only assert 5 monitors. Kept out of `kustomization.yaml` so `kubectl apply -k` still works without the CRDs.
- Grafana dashboard JSON: `deploy/grafana/oss-cash-lab.json` (Grafana 9/10; combined B–F rows; Prometheus datasource `${DS_PROMETHEUS}` / uid `prometheus`). Import: Grafana → Dashboards → Import. Real metric names from bet `/metrics` sources. **No live Grafana**. Parse-only `scripts/check-grafana.sh` on `make smoke` / `make check-grafana` (`json.loads`, panels≥5, PromQL expr grepped against B–F metric source).
- Prometheus alerting rules: `deploy/prometheus/rules.yaml` (plain `groups` for `prometheus --rule.file` / Grafana Mimir) plus `deploy/k8s/prometheusrule.yaml` (`monitoring.coreos.com/v1` PrometheusRule wrapping the same groups). Eight alerts on real B–F names (`McpGatewayCircuitOpen`, `McpGatewayRateLimited`, `AgentCiQueueBacklog`, `AgentCiRunFailures`, `AiBomForbiddenLicense`, `AiBomPolicyHits`, `OtelAiCostHigh` example 50 USD, `CnWorkApprovalsStuck`). **No live Prometheus**. Kept out of `kustomization.yaml` (CRD, like ServiceMonitor). Parse-only `scripts/check-prometheus-rules.sh` on `make smoke` / `make check-prom-rules` (YAML load, ≥6 alerts, PromQL expr grepped against B–F metric source).
- Honest k8s images: `bets/<bet>/Dockerfile` + `.dockerignore` for B/C/D/E/F (`node:20-alpine` B/E, `python:3.12-alpine` C/D/F; EXPOSE 8787/8791/8793/8792/8790; CMD `serve --host 0.0.0.0`; non-root `USER node` / `65532`; Compose `build:` paths unchanged). `scripts/check-dockerfiles.sh` parse-only on `make smoke` / `make check-dockerfiles` (skip `docker build` if Docker missing, like compose-smoke). Build: `docker build -t ghcr.io/wozqhl/<bet>:dev bets/<bet>`
- Image HEALTHCHECK: each B–F `bets/<bet>/Dockerfile` probes `GET /health` on that bet's listen port (B **8787** / C **8791** / D **8793** / E **8792** / F **8790**) via alpine busybox `wget` (`--interval=30s --timeout=3s --start-period=5s --retries=3`). Liveness only — not `/ready` (drain / B circuit / C queue would flap `docker ps`). No `apk add curl`. A has no HTTP serve image. Compose already has service healthchecks; image HEALTHCHECK is the source of truth for `docker ps` / enterprise image reviews. `scripts/check-dockerfiles.py` parse-only (no docker daemon). Does **not** change USER/CMD/EXPOSE, app routes, or k8s/Helm probes.
- Contributing: bilingual `CONTRIBUTING.md`; GitHub issue templates (`bug_report`, `feature_request`) + `pull_request_template` (smoke / local-mvp / bets / security-scan); README link
- Code of Conduct: root [`CODE_OF_CONDUCT.md`](./CODE_OF_CONDUCT.md) (Contributor Covenant 2.1; 中文为主 + short EN; pledge / standards / scope / enforcement warning→ban; supported = this portfolio A–F; no invented inbox — contact maintainers privately, do not file a public issue for harassment; vulns still [`SECURITY.md`](./SECURITY.md)). Hygiene assert in `scripts/check-oss-hygiene.sh` / `make check-oss-hygiene`. README link near CONTRIBUTING / SECURITY.
- Security: root [`SECURITY.md`](./SECURITY.md) (中文为主; GitHub Security Advisories when published under `wozqhl/oss-cash-lab`; until then do not file public issues for vulns / exploit PoCs; local-mvp **0.1.x** only; report fields description / repro / impact / affected bet A–F; six bets + `deploy/`; third-party deps upstream; no invented email; no secrets / B `redactOnWrite` / Apache-2.0 AS-IS). `scripts/security-scan.sh` + `make security-scan` (D ai-bom per bet, secret grep under `bets/`, sample-app allowlisted; `--strict` via `SECURITY_STRICT=1` soft-fail). Hygiene assert hooked from smoke.
- Root orchestration: `make smoke`, `make local-mvp`, `make stack-demo` (B+C+D+E+F HTTP), `make wire-a-to-b`, `make dogfood-a-b`, `make dogfood-a-c`, `make dogfood-a-d`, `make dogfood-a-e`, `make dogfood-a-f`, `make compose-smoke`
- Docs: thesis, ROADMAP, stack-demo, pilot-contract outline; GitHub Actions CI for smoke + local-mvp
- Docs: root README mermaid architecture (six bets + stack ports B 8787 / C 8791 / D 8793 / E 8792 / F 8790 / mock-upstream 8788); `.github/CODEOWNERS` (`* @wozqhl`)
- Dependabot: `.github/dependabot.yml` weekly Monday (github-actions + npm A/B/E + pip C/D/F; open-PR limit 5)
- Copy-paste GitHub Actions examples for A OpenAPI drift + C JUnit + run-vs-run Markdown diff + D SARIF + E GHA annotations under [`examples/github-actions/`](./examples/github-actions/) (not `.github/workflows/`; this repo's gate stays `ci.yml`). `scripts/check-gha-examples.sh` hooked from `make smoke` / `make check-gha-examples`.
- Copy-paste MCP client config for B Streamable HTTP under [`examples/mcp/`](./examples/mcp/) (`url` `http://127.0.0.1:8787/mcp` + Bearer `${env:MCP_GATEWAY_TOKEN}` placeholder; not stdio `command`). `scripts/check-mcp-examples.sh` hooked from `make smoke` / `make check-mcp-examples`. POST-only (`GET /mcp` 405; no SSE). Does not start Cursor.
- GitHub Actions CI: concurrency group per branch/PR (cancel-in-progress); skip dep cache (stdlib-only, no lockfile)

[0.1.0]: https://github.com/wozqhl/oss-cash-lab/releases/tag/v0.1.0
